# Phase 5C-I3 provisioning runbook

Status: **final operational verification pending**. This document records future gates. It does not assert that any live I3 resource exists or authorize provider mutation. Every live provider claim in this document is **operator-supplied evidence, not independently reproducible from this repository** -- nothing here can itself confirm a live account, deployment, credential, or provider response, whether the claim is dated (Gates 4, 5 and 6 below) or still marked NOT VERIFIED (every other gate). Gates 4, 5 and 6 carry dated, redacted, operator-recorded live evidence (2026-09-21, 2026-09-22 and 2026-09-22 respectively, all PASS); every other gate's live provider state remains **NOT VERIFIED**. Strictly read-only Gate 1 inventory was approved by the operational reviewer; provider mutation remains outside this documentation task. Public persistence stays closed throughout I3: ENABLE_PERSISTENT_SUBMISSIONS=false, no Production persistence provider registry/route authorization, no real PostgreSQL or Turnstile launch. A gate is PASS only with dated, redacted evidence from the actual environment and independent acceptance where specified. A failed or unknown gate is STOP.

## Baseline and scope

Expected implementation baseline: d936dd9517e0393c338a08ad63c54b6156804bbe. Before this runbook is committed, HEAD must equal that baseline and the only permitted working-tree artifact is this runbook. After a separately reviewed runbook-only commit, Gate 0 accepts that exact reviewed runbook commit as operational HEAD only if its parent is the implementation baseline and no unrelated source change is present; record both hashes. Any other HEAD requires a new review, not an assumption that a newer commit is safe. Run git rev-parse HEAD, git status --short and git log -1 --format=%H%n%P, then inspect the I3A/I3B checkpoints and the runbook diff. The old notification-cron/migration dirty-tree incident predates this checkpoint and is not a current blocker. Search tracked files and history for accidental real credentials without printing values. Check repository persistence defaults in .env.example and src/lib/persistence-config.ts here; deployed P state is a Gate 1 observation.

The controlling records are PHASE5C_I3A_OPERATOR_SUBMITTER.md, PHASE5C_I3B_INVOCATION_ARCHITECTURE.md, PHASE5C_I2_OPERATIONS.md, deployment/topology.json, deployment/secret-matrix.json, deployment/lifecycle-environment-gates.json, deployment/vercel-project-contract.json and smoke/provider-smoke-plan.json. I3A's historical statement that the invocation channel is missing is superseded by committed I3B code. Its private executor and ambiguity principles remain relevant. I3B's design-time NOT IMPLEMENTED table is likewise superseded for local implementation; its LIVE NOT VERIFIED items remain live gates. Do not interpret local tests as account, IAM, route, durability or schedule proof.

## Exact repository inventory

| Function | Committed paths |
| --- | --- |
| Offline preparation and local submission | scripts/authority-initialize.ts; scripts/authority-rotate-release.ts; scripts/authority-submit.ts; operator/lifecycle-submitter.ts; operator/r2-transport.ts; operator/lifecycle-result.ts; operator/lifecycle-observation.ts |
| Canonical digest, atomic authority receipts and read-only inspection | workers/admission-service/operator-command.ts; workers/admission-service/authority.ts; workers/admission-service/index.ts |
| Lifecycle-only admission and executor | workers/admission-service/index.ts (AuthorityLifecycleOnly and AuthorityLifecycleReadOnly); workers/operator-lifecycle-executor.ts; scripts/authority-executor-preflight.ts; deployment/operator-executor-contract.ts |
| Mailbox, guard and observer | workers/lifecycle-mailbox/index.ts; workers/lifecycle-mailbox/processor.ts; workers/lifecycle-mailbox/dispatch-guard.ts; workers/lifecycle-mailbox/wire.ts; workers/lifecycle-observer.ts |
| Private contracts, templates and gates | deployment/operator-lifecycle-executor.template.jsonc; deployment/lifecycle-mailbox.template.jsonc; deployment/lifecycle-observer.template.jsonc; deployment/lifecycle-transport.production.template.json; deployment/lifecycle-private-contract.ts; deployment/lifecycle-environment-gates.json; scripts/lifecycle-private-preflight.ts |
| Existing I2 deployment and secrets | deployment/admission-service.template.jsonc; deployment/public-signer.template.jsonc; deployment/admin-gateway.template.jsonc; deployment/admission-release-lifecycle.template.json; deployment/ingress-signing-key-rollout.template.json; deployment/secret-matrix.json; deployment/secret-policy.ts; deployment/vercel-project-contract.json; deployment/topology.json; scripts/validate-worker-contracts.ts |
| I2 Workers and application closure | workers/ingress-signer/index.ts; workers/admin-gateway/index.ts; workers/admission-service/service.ts; workers/admission-service/auth.ts; src/lib/persistence-config.ts; .env.example |
| Local fault and integration evidence | tests/authority-submitter.test.ts; tests/operator-executor-contract.test.ts; tests/i3b-authority.test.ts; tests/i3b-cli-result.test.ts; tests/i3b-contract.test.ts; tests/i3b-dispatch-faults.test.ts; tests/i3b-native-transport.test.ts; tests/i3b-result.test.ts; tests/i3b-transport.test.ts; tests/workers/lifecycle-mailbox.integration.ts; tests/workers/operator-executor-rpc.integration.ts; tests/workers/admission-do.integration.ts; tests/workers/production-admission-do-rpc.integration.ts |
| Gate 4 staging admission-service deployment template, preflight and read-only proof (local tooling only; see Gate 4A note below) | deployment/admission-service.staging.template.jsonc; scripts/authority-staging-admission-preflight.ts (`npm run authority:staging:admission:preflight -- --config deployment/<reviewed-staging-admission>.jsonc`); workers/staging-do-rpc-harness.ts; wrangler.staging-do-rpc.local.jsonc; tests/workers/staging-admission-do-rpc.integration.ts; contract tests Q-T in tests/i3b-staging.test.ts |
| Gate 4B execution-safety closure: account-pinned deploy wrapper, live-read-only-proof harness/verifier, rendered-config hygiene (local tooling only; see Gate 4B note below) | scripts/authority-staging-admission-deploy.ts (`npm run authority:staging:admission:deploy:preflight -- --config <path>` / `npm run authority:staging:admission:deploy -- --config <path>`); scripts/authority-staging-admission-live-verify.ts (`npm run authority:staging:admission:live-verify -- --digest <64-hex>`); workers/staging-admission-live-readonly-harness.ts; wrangler.staging-admission-live-readonly.local.jsonc; validateStagingLiveReadonlyHarnessConfig and validateNeverInitializedStagingLifecycleSnapshot in deployment/lifecycle-private-contract.ts; tests/gate4-staging-admission-deploy.test.ts; tests/gate4-live-readonly-contract.test.ts; tests/gate4-rendered-config-hygiene.test.ts; .gitignore (deployment/admission-service.staging.jsonc) |
| Gate 5A deployment-tooling closure: exact rendered filenames, account-pinned deploy wrapper, schedule-inactive CLI enforcement for the executor/mailbox/observer (local tooling only; see Gate 5A note below) | scripts/authority-staging-gate5-deploy.ts (`npm run authority:staging:{executor,mailbox,observer}:deploy:preflight -- --config <path>` / `npm run authority:staging:{executor,mailbox,observer}:deploy -- --config <path>`); exact-filename invariant added to scripts/authority-staging-executor-preflight.ts and to the `staging-mailbox`/`staging-observer` modes of scripts/lifecycle-private-preflight.ts, the latter now also passing `requireScheduleState="STAGING_DEPLOYMENT_INACTIVE"`; tests/gate5-deploy-wrapper.test.ts; tests/gate5-staging-preflight.test.ts; tests/gate5-rendered-config-hygiene.test.ts; .gitignore (deployment/operator-lifecycle-executor.staging.jsonc, deployment/lifecycle-mailbox.staging.jsonc, deployment/lifecycle-observer.staging.jsonc) |
| Gate 6A IAM tooling closure: exact staging transport filename, shared hardened manifest/credential loader, local credential capture, synthetic IAM-test namespace, bounded verification harness (local tooling only; see Gate 6A note below) | exact-filename invariant added to the `staging-transport` mode of scripts/lifecycle-private-preflight.ts and to scripts/authority-staging-submit.ts's manifest loader; operator/staging-credential-io.ts (shared hardened manifest/credential loader, also used by scripts/authority-staging-submit.ts); operator/gate6-iam-namespace.ts; scripts/gate6-secure-input.ts; scripts/gate6-credential-capture.ts (`npm run gate6:credential:capture -- --role <request-write\|result-read> --output <protected path outside repository>`); scripts/gate6-credential-verify.ts (`npm run gate6:credential:verify -- --role <role> --operation <name> --credentials <path> --mode <preflight\|run>`); tests/gate6-staging-transport-preflight.test.ts; tests/gate6-rendered-config-hygiene.test.ts; tests/gate6-namespace.test.ts; tests/gate6-credential-capture.test.ts; tests/gate6-credential-verify.test.ts; tests/workers/support/gate6-capture-harness.ts; tests/workers/support/gate6-verify-harness.ts; .gitignore (deployment/lifecycle-transport.staging.json) |

The checked-in private lifecycle templates and transport manifest are **Production-only and intentionally blocked** by placeholders. The rendered-config validators in deployment/operator-executor-contract.ts and deployment/lifecycle-private-contract.ts pin Production identities. The current scripts/authority-submit.ts loads only deployment/lifecycle-transport.production.json (a future rendered file, absent today); operator/lifecycle-submitter.ts and operator/lifecycle-result.ts also pin Production. The initialization protocol accepts a staging environment, but scripts/authority-initialize.ts pins the authority label used by the Production protocol. Rotation in workers/admission-service/operator-command.ts and scripts/authority-rotate-release.ts is Production-only. **A separately implemented and independently reviewed staging chain, including its signing, target, digest/result and read-only contracts, is prerequisite to any staging provider deployment.** Do not hand-edit Production configs into apparent staging support or use a Production CLI against staging. Staging rotation remains a separate closed gate.

**Gate 4A note (local tooling closure only; no provider mutation performed by it).** The two Gate 4 execution-preflight gaps identified after Gate 3 — no reviewed staging admission-service deployment template/preflight, and no staging-specific runtime/read-only verification harness — are now closed locally: deployment/admission-service.staging.template.jsonc pins the exact staging identity (Worker name limitmark-admission-service-staging; shared main workers/admission-service/index.ts; `AUTHORITY` binding to StagingAdmissionAuthority; migration tag `staging-admission-v1`, distinct from Production's `production-v1`; no route/custom_domains/services/triggers of any kind); a rendered copy (never committed, no real account/key values) is validated with `npm run authority:staging:admission:preflight -- --config deployment/<rendered>.jsonc`, which never deploys or contacts Cloudflare. workers/staging-do-rpc-harness.ts plus wrangler.staging-do-rpc.local.jsonc is a local-only workerd harness (mirroring the existing Production one) driven by tests/workers/staging-admission-do-rpc.integration.ts, proving against a real local Durable Object + SQLite runtime that StagingAuthorityLifecycleReadOnly is genuinely read-only against a never-initialized authority, that the dormant StagingAuthorityLifecycleOnly write entrypoint fails closed (the harness never configures ADMISSION_CURRENT_RPC_KEY, matching the posture a real Gate 4 deployment must have), that rotation stays unconditionally refused, and that all of the above is unchanged across a restart over the same persisted storage. None of this initializes the staging authority, issues a credential, activates a schedule, or performs any provider mutation; Gate 4 itself still requires a live Cloudflare deployment and the live read-only proof against it before it can be marked PASS, and Gates 5-9 remain separately closed exactly as before. A live Cloudflare DO migration, once actually applied, remains provider-persistent; a failed post-deployment verification must be resolved by a corrected redeploy of the same migration tag, never by deleting or renaming the registered class.

**Gate 4B note (execution-safety closure; local tooling only, no provider mutation performed by it).** Two execution-preflight gaps identified during the Gate 4 final preflight are now closed locally, and one hygiene gap is fixed; a fourth item (live account identity verification itself) is explicitly deferred to actual operator execution and is never performed here.

*Account pinning (F1).* The staging admission config's own schema (`validateStagingAdmissionServiceConfig` in deployment/lifecycle-private-contract.ts) has no `account_id` field, unlike every other private config in this repository — that is intentional, not an oversight, so the account is never a value the config itself can pin. The installed Wrangler's own config types (node_modules/wrangler/wrangler-dist/experimental-config.d.mts) document `CLOUDFLARE_ACCOUNT_ID` as the supported non-interactive account-selection mechanism, and `wrangler dev --help` confirms `-l/--local` is what *disables* remote-binding/account resolution (i.e. the account is resolved whenever it is not passed). scripts/authority-staging-admission-deploy.ts is the reviewed wrapper: it requires `CLOUDFLARE_ACCOUNT_ID` in the process environment (never a CLI flag, never added to the config, never logged — only a one-way SHA-256 fingerprint, truncated to 16 hex characters, is ever printed), validates its format (`^[a-f0-9]{32}$`, not all-zero), runs the exact same rendered-config checks as scripts/authority-staging-admission-preflight.ts, and only then constructs `wrangler deploy --config <path>` against the repository-pinned local Wrangler binary (node_modules/wrangler/bin/wrangler.js, spawned directly by path — never `@latest`, never a shell string). It has two modes: `preflight` performs every check above and prints the exact planned command without ever spawning Wrangler; `deploy` performs the identical checks and then, only then, spawns it. Supplying the real account ID: keep it in the operator's own shell environment for the single deploy invocation (e.g. an out-of-repo, non-committed local script or password-manager-sourced export), never in a file under this repository, never typed as a bare literal in a command a shell will keep in history-expanded form. Live confirmation that the supplied ID is in fact the Gate 3 account (as opposed to merely well-formed) is a separate, future, explicitly-live step (`wrangler whoami` immediately before the real deploy) and is intentionally not performed by anything in this repository.

*Live read-only proof (F2).* The Gate 4A harness (workers/staging-do-rpc-harness.ts) only proves read-only behavior against a *local* workerd + SQLite runtime — it cannot prove anything about the live deployed Worker. Installed Wrangler 4.131.1 supports per-binding "remote bindings" in local dev (`services[].remote: true` in the classic JSONC config shape, confirmed both from the shipped type declarations and from `wrangler dev --help`'s own `-r/--remote` vs. `-l/--local` description): `wrangler dev` in its default mode (neither flag) runs the calling Worker's code entirely locally while proxying only bindings marked `remote: true` to the real deployed resource. wrangler.staging-admission-live-readonly.local.jsonc declares exactly one such binding — `LIFECYCLE_READER` → service `limitmark-admission-service-staging`, entrypoint `StagingAuthorityLifecycleReadOnly`, `remote: true` — and nothing else (no routes, no account_id, no other binding); workers/staging-admission-live-readonly-harness.ts is the local-only bridge Worker it belongs to (never deployed, no route, workers_dev/preview false), whose only capability is a single bounded POST endpoint calling `LIFECYCLE_READER.inspectLifecycle(digest)`. scripts/authority-staging-admission-live-verify.ts (`npm run authority:staging:admission:live-verify -- --digest <64-hex>`) is the operator-side CLI: it starts that local `wrangler dev` process (default mode — never `--local`, never `--remote`), performs exactly one inspection call with no retry, stops the process, and applies `validateNeverInitializedStagingLifecycleSnapshot` to the result — an exact-shape check (version 1, environment staging, authorityId staging-public-inquiries-v1, policyEpoch phase5c-i1-epoch-1, initialized false, coverage COMPLETE, status NOT_FOUND, receipt null, releases [], and a bounded ±300000ms observation freshness window) that rejects any mismatch outright, including a live authority that already reports `initialized: true` — it never repairs, resets, retries or falls back to a broader entrypoint. **This script is the one piece of Gate 4B tooling that is expected to contact Cloudflare when actually run** (starting `wrangler dev` against a `remote: true` binding authenticates to and reads from the live account); its live behavior is correspondingly **NOT VERIFIED** by anything in this repository and can only be exercised by the operator, with real Wrangler auth, after the live deploy in Gate 4's own final step.

*Rendered-config hygiene (F3).* .gitignore now excludes exactly `deployment/admission-service.staging.jsonc` (the one rendered filename the preflight/deploy wrapper accept) and nothing broader; `deployment/admission-service.staging.template.jsonc` is unaffected and stays tracked. tests/gate4-rendered-config-hygiene.test.ts proves both facts against git's own `check-ignore` plumbing.

None of the above initializes the staging authority, issues a credential, activates a schedule, deploys Gate 5 (executor/mailbox/observer), or performs any provider mutation. Gate 4 itself still requires the live Cloudflare deploy and the live read-only proof against it (now with reviewed tooling for both, per above) before it can be marked PASS; Gates 5-9 and staging rotation remain separately and identically closed.

**Gate 4 live evidence — 2026-09-21 (environment: staging; PASS).** The human operator performed the live Cloudflare deployment and live read-only proof manually, using only the reviewed Gate 4B tooling above; no provider action in this record was performed by an automated agent, and none of the redacted identifiers below is independently re-verifiable from this repository. Reviewed account fingerprint `0df3a690b3154513` (matches the fingerprint the deploy wrapper's own preflight output showed before the deploy, and the account was confirmed by the operator to match Gate 3's account and CLOUDFLARE_ACCOUNT_ID). A new staging-only Ed25519 operator keypair was bootstrapped locally (offline, never committed); staging public-key fingerprint `74f6e266c7cbdced`; cryptographic self-check reported canonical base64url, 32-byte public key, and matching private/public pair. `deployment/admission-service.staging.jsonc` was rendered from the committed template using only that public key, passed both the standalone preflight and the Gate 4B deploy-wrapper preflight (`status: PASS`, `worker: limitmark-admission-service-staging`, `accountFingerprint: 0df3a690b3154513`, `providerContact: none`), and only the reviewed deploy wrapper was executed against it. Cloudflare reported the Worker `limitmark-admission-service-staging` deployed (version prefix `877ba1f2`) with exactly the `AUTHORITY → StagingAdmissionAuthority` Durable Object binding and the `AUTHORITY_OPERATOR_PUBLIC_KEY` var, no route, no custom domain, `workers.dev` disabled, no R2/service/Queue binding, and no Cron — matching the account's pre-deploy zero-Worker/zero-DO baseline exactly. The reviewed live-read-only verifier (`authority:staging:admission:live-verify`) was run exactly once against a fresh synthetic digest and returned `status: PASS`, `proof: live-never-initialized`, with the live snapshot matching the committed never-initialized contract exactly (version 1, environment staging, authorityId staging-public-inquiries-v1, policyEpoch phase5c-i1-epoch-1, initialized false, coverage COMPLETE, status NOT_FOUND, receipt null, releases []); no retry, mutation fallback, or lifecycle initialization occurred. Post-read Durable Object metrics remained at zero errors and $0.00 billable usage. Gates 5-9, staging rotation, and Production/Vercel state remain unaffected and separately closed; no staging executor/mailbox/observer/guard-state/R2-credential/Cron/lifecycle-object/receipt/release was observed. Live restart/eviction-survival of the deployed Durable Object specifically was not re-exercised live (it remains proven only by the local Gate 4A workerd harness against the identical code path); this is noted as a residual, non-blocking observation, not a PASS condition failure.

The **Production** mailbox and observer templates (deployment/lifecycle-mailbox.template.jsonc, deployment/lifecycle-observer.template.jsonc) contain active one-minute Cron triggers, and their current preflight requires those triggers. A deploy command against those rendered templates could start polling before post-deployment binding inspection. The future Production rollout must therefore have an independently reviewed way to keep schedules inactive until inspection, with matching preflight evidence. This is an unresolved Production deployment gate; the operator must not simply strip triggers from a config that the present validator rejects or assume an empty mailbox makes early activation acceptable.

The **staging** mailbox and observer templates (deployment/lifecycle-mailbox.staging.template.jsonc, deployment/lifecycle-observer.staging.template.jsonc) are the opposite case: they already ship `"triggers": { "crons": [] }` (schedule-inactive) by default, distinct from Production's active-Cron templates above. The remaining staging gap is enforcement, not the template default: scripts/lifecycle-private-preflight.ts's `staging-mailbox`/`staging-observer` modes now require `requireScheduleState = "STAGING_DEPLOYMENT_INACTIVE"` (see the Gate 5A note below), so a rendered staging config carrying an active Cron is refused by the CLI itself, not merely discouraged by convention.

**Gate 5A note (deployment-tooling closure; local tooling only, no provider mutation performed by it).** The Gate 5 local-preflight review found three tooling blockers and one documentation defect; all four are now closed locally, and none of this deploys a Gate 5 resource.

*Exact rendered filenames (F1).* Following the same template-minus-`.template` convention Gate 4 already established, the three reviewed Gate 5 rendered filenames are: `deployment/operator-lifecycle-executor.staging.jsonc`, `deployment/lifecycle-mailbox.staging.jsonc`, `deployment/lifecycle-observer.staging.jsonc`. scripts/authority-staging-executor-preflight.ts and the `staging-mailbox`/`staging-observer` modes of scripts/lifecycle-private-preflight.ts now accept only those exact paths (directory placement *and* basename, both checked after `realpath()` canonicalization, so a symlink at the right name pointing elsewhere resolves to its real target's own dirname/basename and is rejected the same as any other alternate name); Production modes (`mailbox`/`observer`/`transport`) and `staging-transport` are unchanged, since this repository has no reviewed deployment plan for those yet.

*Account-pinned deploy wrapper (F5).* scripts/authority-staging-gate5-deploy.ts generalizes the Gate 4B deploy-wrapper pattern to the three Gate 5 Workers via one narrow, closed-set `<resource>` selector (`executor`/`mailbox`/`observer` only — there is no `all`/`*` value and no combined "deploy all three" command anywhere in this repository); each resource hardcodes its own exact Worker name, exact rendered filename, exact main path and exact validator. It requires `CLOUDFLARE_ACCOUNT_ID` in the process environment (never a CLI flag, never logged — only a one-way SHA-256 fingerprint, truncated to 16 hex characters), and has the same two modes as Gate 4B: `preflight` performs every check and prints the exact planned command without ever spawning Wrangler; `deploy` performs the identical checks and then, only then, spawns the repository-pinned local Wrangler binary with exactly `deploy --config <path>` — no `--env`, `--route`, `--var`, binding/domain override, alternate name or alternate config is constructible. Commands: `npm run authority:staging:executor:deploy:preflight -- --config deployment/operator-lifecycle-executor.staging.jsonc` (and the `mailbox`/`observer` equivalents), each with a matching `:deploy` command that actually spawns Wrangler once real credentials are available. None of these commands has been run against a live account by anything in this repository; provider mutation via them is future operator action only.

*Schedule-inactive CLI enforcement (F4).* The staging mailbox/observer preflight (both the standalone script and the deploy wrapper) now calls `validateStagingLifecycleMailboxConfig`/`validateStagingLifecycleObserverConfig` with `requireScheduleState = "STAGING_DEPLOYMENT_INACTIVE"` explicitly, rather than merely reporting whichever of the two reviewed schedule states a config happens to carry. A rendered config with `"triggers": { "crons": [] }` passes; any active or malformed Cron shape is refused by the CLI itself, closing the gap the Gate 5 preflight review identified.

*Deployment order (F6).* scripts/authority-staging-gate5-deploy.ts deploys exactly one Worker per invocation by construction — there is no combined command. The reviewed order is **executor → inspect → mailbox (with its `StagingLifecycleDispatchGuard` registration) → inspect → observer → inspect**, each step followed by dashboard binding/exposure inspection before the next Worker is deployed, mirroring the Gate 4 evidence pattern (A5/A6 in this runbook).

*Capability graph (F7).* Unchanged from the committed contracts: executor binds only `ADMISSION_SERVICE → limitmark-admission-service-staging / StagingAuthorityLifecycleOnly`, no DO/R2/Cron/route; mailbox binds `LIFECYCLE_EXECUTOR`, `LIFECYCLE_READER`, the two Gate-3 R2 buckets, and its own `DISPATCH_GUARD → StagingLifecycleDispatchGuard` (migration `i3b-staging-guard-v1`), Cron inactive, no route; observer binds `LIFECYCLE_READER` and the two R2 buckets only, no DO, no write-capable service, Cron inactive, no route. None of them may carry `ADMISSION_CURRENT_RPC_KEY` (forbidden by deployment/secret-matrix.json's `operatorExecutor`/`lifecycleMailbox`/`lifecycleObserver` policies), external R2 API credentials, a staging private signing key, or any lifecycle-initialization/rotation capability — Gate 5 deploys and wires only; Gate 6 issues R2 credentials, Gate 7 adds `ADMISSION_CURRENT_RPC_KEY` to the admission Worker and activates schedules/initializes the authority, and Gate 9 (staging rotation) stays permanently closed.

*Git hygiene (F2).* .gitignore now excludes exactly the three rendered filenames above; their `.template.jsonc` sources stay tracked. As with Gate 4B, this is a hygiene backstop, not the primary safety boundary — the exact-filename invariant in the preflight/deploy tooling itself is.

*Plan entitlement.* Gate 4 proved one SQLite Durable Object class works on the current Workers Free plan at $0.00; the mailbox's `StagingLifecycleDispatchGuard` would be a **second** DO class, and neither this repository nor anything consulted while building this tooling establishes a live-verified limit on DO classes per account on the current plan. **This remains REVERIFY BEFORE MUTATION**: the operator must confirm on the dashboard, immediately before the mailbox deploy, that registering a second class does not require a plan change, and must not proceed past a plan-limit warning if one appears.

None of the above deploys a Gate 5 Worker, issues a credential, activates a schedule, or performs any provider mutation. Gate 5 provider mutation itself remains blocked pending the operator actually running the sequence above with real credentials, in order, with inspection between each step.

**Gate 5 live evidence — 2026-09-22 (environment: staging; PASS).** The human operator performed the live executor, mailbox and observer deployments manually, using only the reviewed Gate 5A tooling above, in the reviewed order (executor -> inspect -> mailbox -> inspect -> observer -> inspect); no provider action in this record was performed by an automated agent, and none of the redacted identifiers below is independently re-verifiable from this repository or by any session without live Cloudflare access. Reviewed account fingerprint `0df3a690b3154513` and staging public-key fingerprint `74f6e266c7cbdced` matched the values already recorded at Gate 4. The pre-live baseline reported exactly one existing Worker (`limitmark-admission-service-staging`), one SQLite DO namespace, and both Gate 3 R2 buckets empty. `deployment/operator-lifecycle-executor.staging.jsonc`, `deployment/lifecycle-mailbox.staging.jsonc` and `deployment/lifecycle-observer.staging.jsonc` were each rendered from their committed templates, confirmed gitignored, and passed both the standalone and Gate 5A deploy-wrapper preflights (`status: PASS`, `providerContact: none`) before any deploy wrapper was executed.

Cloudflare reported all three deployments succeeded: `limitmark-authority-operator-executor-staging` (version prefix `d9861396`) with exactly the `ADMISSION_SERVICE -> limitmark-admission-service-staging / StagingAuthorityLifecycleOnly` binding and no DO/R2 binding; `limitmark-lifecycle-mailbox-staging` (version prefix `ec9d712e`) with exactly the five bindings `DISPATCH_GUARD -> StagingLifecycleDispatchGuard`, `REQUEST_BUCKET`, `RESULT_BUCKET`, `LIFECYCLE_EXECUTOR -> limitmark-authority-operator-executor-staging / StagingOperatorLifecycleExecutor` and `LIFECYCLE_READER -> limitmark-admission-service-staging / StagingAuthorityLifecycleReadOnly`; `limitmark-lifecycle-observer-staging` (version prefix `6ed427a5`) with exactly `LIFECYCLE_READER`, `REQUEST_BUCKET` and `RESULT_BUCKET`, no DO and no write-capable service. All three showed no route, `workers.dev` disabled, no custom domain, no preview URL, zero invocations and zero errors, matching the committed capability graph exactly. The final topology showed exactly four Workers, all with no active routes, billable usage $0.00.

Durable Objects showed exactly two SQLite namespaces after the mailbox deploy: the existing admission namespace and a new `StagingLifecycleDispatchGuard` namespace bound only to the mailbox, reporting zero requests/errors/duration (class/migration registered, guard business state not created). Both Gate 3 R2 buckets remained empty after all three deployments. The reviewed live-never-initialized verifier (`authority:staging:admission:live-verify`) was run exactly once against a fresh synthetic digest after the Gate 5 deployments and returned `status: PASS`, `proof: live-never-initialized`, matching the committed never-initialized contract exactly (version 1, environment staging, authorityId staging-public-inquiries-v1, policyEpoch phase5c-i1-epoch-1, initialized false, coverage COMPLETE, status NOT_FOUND, receipt null, releases []); no retry, mutation fallback or lifecycle initialization occurred. Account-wide SQL rows written remained 0, total SQL storage 0 B, KV storage 0 B, billable usage $0.00. No schedule was activated, no external R2 API credential was created, and ADMISSION_CURRENT_RPC_KEY, a staging private signing key, lifecycle request/result objects, guard claim state, staging authority initialization and staging rotation all remained absent. Gates 6, 7 and 9 remain unaffected and separately closed; Production and Vercel state were not intentionally mutated by any Gate 5 action.

**Gate 6A note (IAM tooling closure; local tooling only, no credential is created, revoked or negatively tested against a live account by anything in this section).** Before any real Gate 6 R2 credential is issued, the following local closure makes Gate 6 safe for independent review. None of it performs a provider mutation, and Gate 6 itself -- live credential issuance and its live negative/revocation tests -- remains open exactly as before; it can only be closed by the operator actually issuing credentials against the live Gate 5 buckets and running the tooling below against them. (Superseded: the operator has since done exactly that -- see the Gate 6 live evidence and Gate 6 closure adjudication paragraphs below, which record Gate 6 PASS.)

*Staging transport exact file contract (F1).* Following the same pattern Gate 4B pinned for the admission config and Gate 5A pinned for the three Gate 5 Worker configs, `deployment/lifecycle-transport.staging.json` is now pinned the identical way. The `staging-transport` mode of scripts/lifecycle-private-preflight.ts, previously the one mode left on directory-only placement (see the Gate 5A note above), now also requires the exact basename `lifecycle-transport.staging.json`, checked after `realpath()` canonicalization exactly like every other exact-filename mode -- an alternate basename, the raw `.template.json`, a symlink resolving elsewhere, or a config placed outside `deployment/` are all refused. scripts/authority-staging-submit.ts's own manifest loader (used at the point the R2 request is actually signed, not only at preflight) received the identical realpath/dirname/basename check as defense in depth, and that loader and the credential-file reader were factored into a new shared module, operator/staging-credential-io.ts, so this one reviewed implementation is now reused by both the staging submitter and the new Gate 6A verification harness below rather than risking two independently-drifting copies. A Production-shaped manifest is refused by content (`environment !== "staging"` among other pinned fields) even if placed at the exact staging path. .gitignore now excludes exactly `deployment/lifecycle-transport.staging.json`; `deployment/lifecycle-transport.staging.template.json` stays tracked -- a hygiene backstop only, as with Gates 4B/5A. tests/gate6-staging-transport-preflight.test.ts and tests/gate6-rendered-config-hygiene.test.ts prove all of the above; tests/i3b-staging.test.ts and tests/i3b-transport.test.ts (unchanged) continue to pass against the refactored shared loader.

*Staging transport contract review (F2).* validateStagingLifecycleTransportManifest (deployment/lifecycle-private-contract.ts) was inspected against the exact pinned fields required above (version, environment, accountId, requestBucket, resultBucket, authorityId, policyEpoch, operatorPublicKey): it already rejects any unknown field (exact-key allowlist), pins every field but accountId/operatorPublicKey to a Production-distinct staging literal, and validates accountId as exactly 32 lowercase hex characters, not all-zero. No concrete gap was found; nothing was strengthened, since strengthening a validator the committed contract does not require would itself be an unreviewed change to a safety-relevant contract.

*Credential file custody (F3).* The existing strict two-key reader (now `readStagingR2Credential` in operator/staging-credential-io.ts, used identically by the submitter and the Gate 6A harness) already enforced strict UTF-8-without-BOM JSON, exactly the two nonempty string members accessKeyId/secretAccessKey, and a 1,024-byte bound -- but nothing in this repository could safely *write* such a file before now. scripts/gate6-credential-capture.ts is the new local capture tool; scripts/gate6-secure-input.ts is the one place a real invocation ever reads a keystroke. `--role request-write|result-read --output <path>` is mandatory with no default and no combined mode -- the two credentials are always captured to two separate files by two separate invocations. The output path's *parent directory* is resolved with `realpath()` and refused outright if it is the repository root or nested under it (including through a symlink, since the comparison is against the resolved, not literal, parent); the parent directory must already exist (this tool never creates one); the file is opened with the exclusive-create flag so an existing file is never overwritten, with no `--force`; POSIX file mode is set to owner-only (`0o600`) best-effort (not fully enforceable on every platform). Both fields are read with no terminal echo at all (not even a mask character) and refuse outright on a non-interactive stdin -- there is no non-TTY fallback, so a secret can never reach argv, an environment variable, a shell history, or a log. The confirmation printed afterward names the role, the output path and a bounded one-way SHA-256 fingerprint (12 hex characters) of accessKeyId; neither raw value is ever printed. tests/gate6-credential-capture.test.ts exercises all of this at the CLI level through an esbuild-substituted synthetic version of gate6-secure-input.ts (mirroring the existing r2-transport substitution pattern in tests/workers/support/i3b-cli-result-harness.ts) supplying clearly-synthetic values with no real TTY involved; the real masked-input code path itself can only be exercised by an interactive operator.

*Synthetic Gate 6 IAM-test namespace (F4).* operator/gate6-iam-namespace.ts defines the dedicated `gate6-iam-test/<32-hex-nonce>.json` key format (bounded to at most 256 bytes, content `{version, purpose: "gate6-iam-test", nonce, createdAtMs}`), which cannot syntactically collide with any real lifecycle key (`initialize.json`, `rotate-release.json`, `settle.json`, `reconcile.json`, `lifecycle/<digest>.json`, `settlement/<nonce>.json`, `reconciliation/<nonce>.json` -- none of which carry this prefix or this content shape). tests/gate6-namespace.test.ts proves the namespace is syntactically disjoint from every real key shape and, more directly, that the staging mailbox (workers/lifecycle-mailbox/staging-processor.ts, unchanged by this work) issues exactly two `REQUEST_BUCKET.get()` calls -- the fixed literals `"initialize.json"` and `"settle.json"` -- and never a Gate 6 IAM-test key, so the mailbox is structurally incapable of interpreting one as a lifecycle command regardless of what exists in the bucket. Cleanup ownership: request-write fixtures are cleaned up by the provider administrator (or left to the bucket's existing 24-hour request-object retention); the one result-bucket fixture is administrator-owned per F8 below.

*Gate 6 IAM verification harness (F5-F7, F9).* scripts/gate6-credential-verify.ts is the new bounded, closed-set local/operator-side CLI. It reuses the single reviewed SigV4 primitive (operator/r2-transport.ts's `oneR2Request`) and the single reviewed staging manifest loader -- there is no operator-suppliable URL, bucket, key, method, or environment anywhere in its argument surface. `--role request-write|result-read` and `--operation <name>` each select from one fixed table (below); `--mode preflight` resolves and prints the exact planned request without ever calling the transport; `--mode run` performs exactly the one bounded call the resolved operation names, then classifies the observed HTTP status (2xx GRANTED, 403 DENIED, anything else AMBIGUOUS) against that operation's fixed expectation and reports MATCH, MISMATCH (an excess or missing right -- treat as STOP per the credential custody matrix), or AMBIGUOUS. Every request/response byte and secret stays local; only the role, operation, bucket name, key, expectation, observed result, HTTP status and a one-way accountId fingerprint are ever printed.

  Request-write table: `write-request-fixture` (PUT request bucket, expect ALLOW) is the one allowed operation; `deny-result-write` (PUT result bucket), `deny-result-read` (GET result bucket), and `deny-cross-environment-write` (PUT the literal Production request bucket name) all expect DENY. Result-read table: `read-result-fixture` (GET result bucket at an operator-supplied `--fixture-nonce`, expect ALLOW) is the one allowed operation; `deny-result-write`, `deny-request-write`, `deny-request-read`, and `deny-cross-environment-read` (GET the literal Production result bucket name) all expect DENY. This is derived literally from the credential custody matrix and the Request/Result credential scope rows in the Live provider checks table below: request-bucket GET/LIST/DELETE denial for the request-write credential is deliberately **not** asserted, since the committed contract does not require it (inventing a stricter rule was explicitly out of scope for this closure); Worker/config/token-management denial is **not expressible** through this R2 SigV4 surface at all and is carried forward to the Provider Permission Expressiveness paragraph below rather than silently assumed.

  Revocation procedure (F9; adjudication corrected by the Gate 6D note below -- this paragraph is the current, superseding text). Run the one allowed operation once (expect MATCH/GRANTED, HTTP 2xx), have the provider administrator revoke the credential, then run the *same* operation once more with the same credential file, role and target. This is a distinct adjudication from the ordinary IAM negative-permission classifier immediately above and must **not** reuse its "observed DENIED" rule as the sole revocation signal: that classifier's 403-only DENIED is correct and unchanged for every ordinary allow/deny probe (`deny-result-write`, `deny-result-delete`, `deny-request-write`, `deny-request-read`, `deny-cross-environment-write`, `deny-cross-environment-read`), where 401 must keep reading AMBIGUOUS, since a 401 there could just as easily mean a broken or mistyped local credential file as a real policy boundary, and treating it as DENIED there would let a dead credential masquerade as a proven negative-permission result. Revocation is different: the operator reads the raw `httpStatus` (not only the generic `observed` field) on the post-revocation call. Per official Cloudflare R2 S3-API error semantics -- **operator-supplied current provider documentation, not independently verifiable from this repository** -- HTTP 401/Unauthorized means the credential no longer authenticates at all, and HTTP 403/AccessDenied means it authenticates but is no longer authorized; either one, on its own, is valid evidence that the exact previously-working operation is no longer effective, and therefore that revocation succeeded (a 401 is, if anything, the *stronger* proof, since it shows the token itself was invalidated rather than merely re-scoped). A repeated HTTP 2xx (`status: MATCH`, `observed: GRANTED`) on the post-revocation call is the STOP condition (ineffective revocation). Any other outcome (404, 400, 405, 409, 429, 5xx, timeout/transport failure) is inconclusive -- it proves neither that revocation happened nor that it failed -- and must not be recorded as either PASS or STOP; if it prevents PASS evidence, treat the credential as not yet confirmed revoked and escalate to the administrator rather than reinterpreting the status code. The tool performs at most one bounded call per invocation with no internal retry, so a revocation check can never itself create a second write/read beyond the one the operator explicitly re-invokes; no code change was made to `classify()` or to any operation's fixed expectation to support this -- the tool already prints the raw `httpStatus` the operator needs, and the adjudication above is documentation-only.

  tests/gate6-credential-verify.test.ts exercises all of the above -- role separation, the closed operation allowlist, ALLOW/DENY/MISMATCH classification, that preflight never invokes the transport, that each invocation performs exactly one bounded call, and that no secret or raw accountId is ever printed -- through the same esbuild-substitution pattern (tests/workers/support/gate6-verify-harness.ts), driven by a synthetic transport that never opens a socket and answers from a per-test allow-list simulating an issued credential's effective policy.

**Gate 6C note (result-bucket DeleteObject denial and request-write terminology closure; still local tooling only, no live probe run by anything in this section).** The F5-F7 table above had no DELETE probe at all, so an "Object Read only" preset accidentally issued with delete rights would have gone unnoticed. `deny-result-delete` (DELETE the result bucket, expect DENY) is now a required result-read operation in the same closed `operationsByRole` table as every other entry above -- no operator-suppliable method, bucket or key, and no LIST support was added, since the committed contract does not require a live LIST probe. It always targets a freshly generated `gate6-iam-test/<32-hex>.json` key, distinct from the F8 positive fixture (which only `read-result-fixture --fixture-nonce` ever touches) and from every real lifecycle key, and is expected not to exist: a mistaken GRANTED result therefore deletes nothing of value. Classification is unchanged and unexpanded -- 2xx GRANTED, 403 DENIED, anything else (404, 400, 401, 405, 409, 429, 5xx, timeout, DNS/transport failure) AMBIGUOUS or UNAVAILABLE, never a silent PASS -- and the tool still performs at most one bounded call per invocation, with no retry. operator/r2-transport.ts's method union was widened from `PUT | GET` to `PUT | GET | DELETE` on the single reviewed SigV4 path; no second signer or arbitrary-method surface was added, and Production callers (scripts/authority-submit.ts, scripts/authority-staging-submit.ts) are unaffected since they still only ever call PUT/GET. tests/i3b-transport.test.ts and tests/gate6-credential-verify.test.ts cover the new DELETE path and probe.

Separately, `request-write`'s operator-facing label in scripts/gate6-credential-capture.ts previously read "intended scope: request-bucket object PUT only", which overstated the actual contract above: request-bucket GET/LIST/DELETE denial for the request-write credential has never been asserted here, because a normal Cloudflare "Object Read & Write, specific bucket" preset grants those coarse same-bucket rights and the committed contract does not require a stricter one. The label now says PUT is the operation this pipeline uses and that it does not rely on any coarser same-bucket right the provider preset may also grant, without claiming a PUT-only credential shape this repository cannot request or independently verify. This is a wording correction only; the role identifier `request-write` and its allowed-operation table are unchanged.

The full request/result live allow-deny matrix -- including this new `deny-result-delete` probe -- is **operator-supplied evidence, not independently reproducible from this repository**, exactly like every other live Gate 6 claim in this document; nothing in this closure contacts Cloudflare.

**Gate 6 live evidence — 2026-09-22 (environment: staging; operator-supplied).** The operator ran the complete closed operation table for both credentials against the live Gate 5 buckets, one credential at a time, in the reviewed F3/order-of-operations sequence. Every DENY-expected operation observed DENIED (HTTP 403); every ALLOW-expected operation observed GRANTED (HTTP 200); no operation returned MISMATCH, AMBIGUOUS or UNAVAILABLE. Redacted per the credential custody matrix -- no account ID, access key ID, secret, credential file path, full fixture nonce/key or OAuth data below, only the closed table's own operation names and outcomes:

Request-write: `write-request-fixture` (staging request PUT) HTTP 200 GRANTED, expect ALLOW, MATCH; `deny-result-write` (staging result PUT) HTTP 403 DENIED, MATCH; `deny-result-read` (staging result GET) HTTP 403 DENIED, MATCH; `deny-cross-environment-write` (Production request PUT) HTTP 403 DENIED, MATCH.

Result-read: `read-result-fixture` (GET the exact synthetic result fixture from F8) HTTP 200 GRANTED, expect ALLOW, MATCH; `deny-result-write` (staging result PUT) HTTP 403 DENIED, MATCH; `deny-result-delete` (staging result DELETE) HTTP 403 DENIED, MATCH; `deny-request-write` (staging request PUT) HTTP 403 DENIED, MATCH; `deny-request-read` (staging request GET) HTTP 403 DENIED, MATCH; `deny-cross-environment-read` (Production result GET) HTTP 403 DENIED, MATCH.

All account fingerprints observed during this matrix matched the reviewed staging account fingerprint already recorded at Gates 4/5. The result-read credential's revocation evidence is recorded separately in the Gate 6D note below (200 pre-revoke, provider revocation, 401 post-revoke, REVOCATION PASS). Per the Gate 6 summary row above, only result-read underwent a revocation check; the request-write credential was tested for its allow/deny matrix only and was not revoked -- see the rationale below and the Gate 6D note. With this recorded, every itemized live-probe and revocation requirement in the Live provider checks table below has operator-supplied PASS evidence, including independent policy review -- see the Gate 6 closure adjudication below, which records **Gate 6 PASS**.

**Gate 6D note (revocation adjudication and result-read revocation evidence; documentation-only, no code changed, no live probe run by anything in this section).** The F9 paragraph above previously read the post-revocation call's generic `observed` field and required it to say DENIED, which implicitly assumed a revoked credential always re-fails with HTTP 403. A live result-read revocation check instead observed HTTP 401 on the post-revocation call (`observed: AMBIGUOUS` under the unchanged, correctly conservative ordinary classifier). F9 above is now corrected to adjudicate revocation from the raw `httpStatus` (401 or 403, either one) rather than from the ordinary classifier's `observed` field alone; this correction is documentation-only -- `scripts/gate6-credential-verify.ts`'s `classify()` function, its fixed operation table, and every ordinary allow/deny probe's requirement of HTTP 403 for DENIED are all unchanged, and 401 still reads AMBIGUOUS for every one of them, exactly as before. The result-read sequence itself, recorded in redacted form only (no credential identifier, account/bucket name beyond what is already public in this document, or fixture key): the exact positive result fixture read HTTP 200 (GRANTED/MATCH) before revocation; the provider administrator then revoked only the result-read token; the identical GET, same credential file, role, account and fixture key, then read HTTP 401 (AMBIGUOUS under the ordinary classifier); no retry was performed. Applying the corrected adjudication above, and given the operator-supplied provider-semantics premise that 401 specifically denotes an invalidated credential: **this is recorded as REVOCATION PASS for the result-read credential.** Two points remain outside what this repository's tooling can itself confirm and stay operator-supplied: that the second invocation truly reused the identical credential file, role and target as the first (the tool is stateless across separate invocations by design -- see F9 -- and has no run-identity mechanism linking two printed JSON lines beyond an operator retaining and comparing them, e.g. by matching the `accountFingerprint` and `key` fields the tool did print on both calls), and the provider's own 401-vs-403 semantics themselves, which are read from current Cloudflare documentation rather than anything committed in this repository.

The **Request credential scope** row and the **Result reader scope and revocation** row in the Live provider checks table below are, as committed, asymmetric: only the result-read row specifies "revoke test token and re-test" and "observed revocation timing" as required PASS evidence; the request-write row's only revocation-related text is "Any excess right: revoke, STOP" -- revocation there as a remedial action on a finding, not a scheduled qualification step. The Gate 6 summary row above them previously said only "test revocation" without naming a credential; it now reads "test result-read revocation per itemized checks", resolving the ambiguity this note originally flagged in favor of the itemized table's own asymmetry rather than an independent requirement. A request-write revocation cycle is **not required for Gate 6**, precisely because the request-write credential must remain active through Gate 6 and into Gate 7's initialization PUT (Gate 7's PASS evidence requires "observe PUT acceptance" for a signed staging initialization, which needs a live, unrevoked request-write credential). Nothing here revokes, tests, or otherwise touches the request-write credential; it remains active.

A related gap surfaces once Gate 7 is reached: Gate 7's own PASS evidence requires "exact stage receipt via read-only reconciliation" (`authority-staging-submit.ts read-result`/`reconcile`), which needs a *working* result-read R2 credential -- but the credential just recorded as PASS above is now, by that same evidence, no longer authenticated. The committed runbook does not explicitly state that a fresh result-read credential must be issued and captured before Gate 7's reconciliation step; this is inferred from the two requirements' own logic, not stated. **This is the minimum documentation clarification needed before Gate 7**, and it is a Gate 7 prerequisite, not a reason to withhold the Gate 6 PASS just recorded for the (deliberately, successfully) revoked result-read credential.

**Gate 6 closure adjudication -- PASS.** With the live evidence above, every itemized entry in the Live provider checks table's Request credential scope and Result reader scope and revocation rows has operator-supplied PASS evidence, and no further live provider probe is required. The Gate 6 summary row's PASS-evidence column names "Provider audit and redacted allow/deny results, no Global API Key; independent policy review"; all three are now satisfied:

*Redacted allow/deny results:* satisfied by the Gate 6 live evidence paragraph above.

*Independent provider-policy review, request-write.* A reviewer independent of the operator who issued and tested the credential separately inspected the live token configuration directly in the provider dashboard (not merely the tool's effective-policy probes) and confirmed: an Account API Token, named `limitmark-gate6-request-write-staging`, status Active, permission preset "Object Read & Write," restricted to the specific bucket `limitmark-lifecycle-requests-staging` only (not "apply to all buckets"), with no broader Admin Read/Read & Write or account-wide preset selected. No Access Key ID, Secret Access Key, bearer token, account ID or other secret/private material was recorded or is reproduced here -- token name, permission preset and bucket scope are the only non-secret configuration facts kept, consistent with the redaction posture elsewhere in this document. This independently confirms the dashboard-configured policy matches the effective allow/deny matrix already recorded above, rather than relying on the effective-policy probes alone.

*Independent provider-policy review, result-read.* The result-read token was revoked before this independent review, so no live dashboard configuration remains to inspect; its provider policy is instead established by its own reviewed effective-policy matrix above (one GET-only ALLOW, five DENYs spanning result-write, result-delete, request-write, request-read and the cross-environment probe) captured while the token was still active, which independent review accepted as bounding the credential to the required result-read capability before revocation.

*No Global API Key:* both credentials were issued and independently confirmed as scoped Account API Tokens with a specific-bucket, specific-permission preset (Object Read & Write / Object Read only) -- a shape the Global API Key, which carries no per-bucket or per-permission preset at all, cannot take. Independent review found this, together with the reviewed R2 SigV4 Account-API-Token workflow this tooling exclusively targets, sufficient to establish no Global API Key was used.

*Independent policy review:* PASS, per the two dashboard/matrix confirmations above, performed by a reviewer independent of the operator who issued and tested both credentials.

**Gate 6 therefore PASSES.** Every itemized live probe, the result-read revocation, and the independent policy review are complete; no further live provider action is required to close Gate 6. This does not initialize the staging authority, activate any schedule, or touch Gate 7-9, all of which remain exactly as closed as before this closure. The request-write credential remains **active** (required for Gate 7's staging-initialization PUT; it was not revoked and is not tested for revocation, per the asymmetry paragraph above); the result-read credential's PASS revocation does **not** invalidate this Gate 6 PASS; and, per the related-gap paragraph above, a **fresh** result-read credential -- not yet issued -- is a Gate 7 prerequisite for reconciliation/read-result activity, not Gate 6 work and not something this closure issues or assumes exists.

*Trusted result fixture bootstrap (F8).* The result-read credential must never create its own positive-read fixture (that would broaden it beyond result-bucket GET). The reviewed procedure is: the provider administrator creates exactly one harmless synthetic object directly through an administrator-controlled provider surface (the R2 dashboard, or an administrator's own separately-scoped credential -- never the result-read credential under test), under the `gate6-iam-test/` namespace, with the exact content shape `gate6IamTestFixtureBody` produces (`{version: 1, purpose: "gate6-iam-test", nonce, createdAtMs}`, at most 256 bytes) and a nonce the operator records. `read-result-fixture --fixture-nonce <that nonce>` is then the one ALLOW test. This necessarily remains a manual dashboard (or administrator-credential) action -- no code in this repository creates it, matching the instruction not to broaden the tested credential to enable its own positive test. Cleanup: the administrator deletes the fixture directly (the result-read credential under test is not expected to hold delete permission, so it cannot and must not be asked to clean up its own fixture); absent prompt administrator cleanup, the object ages out under the bucket's existing 30-day result retention.

*Provider permission expressiveness boundary (F10).* Nothing was contacted live for this section; only locally installed Wrangler was inspected. `wrangler r2 --help` / `wrangler r2 bucket --help` (installed Wrangler, this repository's pinned version) expose bucket lifecycle/CORS/lock/notification/domain management commands but **no command to create, inspect, or scope an R2 API (S3-compatible) token** -- confirming what the existing credential custody matrix already assumed: R2 API credential issuance and its permission granularity are dashboard/API-only and outside anything this repository's local tooling can create, inspect, or verify ahead of time. The following remain **live/provider-documentation questions this repository cannot answer locally** and must be reverified against the actual Cloudflare token-creation UI/API at live issuance time, before this closure can be treated as sufficient: whether bucket-level scope is expressible as a hard boundary or only a default; whether object-level read vs. write can be split as two distinct permissions or arrive coupled; whether list is a separate permission from get; whether delete is a separate permission from put; whether prefix-level restriction exists at all; whether cross-bucket isolation is enforced by the token model itself or only by bucket-name selection; and whether token-management/Worker-management capability is structurally excluded from an R2-scoped token or must be independently confirmed absent. Per the instructions for this closure, the harness above fails closed rather than silently relaxing the project contract: every operation it can run is one of the fixed table entries above, so a provider permission model that cannot express the required ALLOW/DENY split will surface as a MISMATCH (or, if outright inexpressible, as the operator's own live observation), never as a passing test.

*Secret incident model (F11).* Documented, not automated (there is no code path in this repository that could safely automate a real revocation): a secret printed, committed, or pasted into evidence/chat is revoked immediately, in every case, with no exception; a wrong-scope credential is revoked and reissued rather than reused; if one of the two Gate 6 credentials is created and the second issuance fails, the first may remain only if it has already been verified (via this harness) and is protected (captured per F3, never logged) -- otherwise it is revoked too rather than left half-verified; a credential that cannot be re-displayed by the provider is expected behavior, not a bug -- protected capture (scripts/gate6-credential-capture.ts) must happen once, at creation time; a local capture failure *after* provider issuance (e.g. the capture tool refuses the chosen path) is resolved by revoking and reissuing to a corrected path, never by improvising an unreviewed local workaround or retrying over a half-written file (scripts/gate6-credential-capture.ts already refuses to overwrite, with no `--force`, precisely so this failure mode cannot be silently retried).

*One credential at a time (operational note).* Nothing in this repository issues, requests, or expects two credentials simultaneously; the runbook's own reviewed order is: administrator issues the request-write credential, operator captures it (F3), operator verifies its allow/deny matrix (F5-F7); only then does the administrator issue the result-read credential, operator captures it, operator verifies its matrix (including the F8 fixture read). This is a documentation convention this closure follows, not new enforcement code -- the two `--role` values already make it impossible to request, capture, or verify both under one invocation.

None of the F1-F11 tooling above itself creates or revokes a real R2 credential, contacts Cloudflare, or performs a provider mutation -- it is local tooling only, exactly as before. At the time this Gate 6A closure was originally written, Gate 6 itself -- live credential issuance, live negative testing, and live revocation timing -- was still open, pending the operator actually issuing credentials against the live Gate 5 buckets and running scripts/gate6-credential-capture.ts and scripts/gate6-credential-verify.ts against them, one credential at a time, in the order above. The operator has since done exactly that: see the Gate 6 live evidence and Gate 6 closure adjudication paragraphs above, which record **Gate 6 PASS**. Gate 7 (schedule activation and initialization) remains closed and unaffected; nothing in this closure, nor the Gate 6 PASS just recorded, adds `ADMISSION_CURRENT_RPC_KEY`, activates a Cron, or initializes the authority.

## Future resource inventory: keep environments separate

All names below come from deployment/lifecycle-environment-gates.json unless identified as I2 Production resources. Names are intended identities, not evidence of existence. Account ID, actual namespaces/bucket IDs, provider plan, secrets, key fingerprints, deployed versions, schedules and binding resolution are **OPERATOR INPUT REQUIRED / LIVE NOT VERIFIED**.

| Resource or capability | STAGING | PRODUCTION | Closure requirement |
| --- | --- | --- | --- |
| Private request R2 bucket | limitmark-lifecycle-requests-staging | limitmark-lifecycle-requests-production | No public access; 24-hour input lifecycle; fixed four keys only read by code |
| Private result R2 bucket | limitmark-lifecycle-results-staging | limitmark-lifecycle-results-production | No public access; 30-day result lifecycle |
| Mailbox Worker | limitmark-lifecycle-mailbox-staging | limitmark-lifecycle-mailbox-production | Scheduled only; fetch 404; no route, workers.dev, preview URL, assets |
| Guard namespace/class/object | Separate SQLite DO namespace; LifecycleDispatchGuard; staging-lifecycle-dispatch-v1 | Separate namespace; LifecycleDispatchGuard; production-lifecycle-dispatch-v1 | Only mailbox owns DISPATCH_GUARD; durable consumed ledger and supervisor latch |
| Observer Worker | limitmark-lifecycle-observer-staging | limitmark-lifecycle-observer-production | Scheduled only; fetch 404; no executor/guard/authority binding |
| Executor Worker | limitmark-authority-operator-executor-staging | limitmark-authority-operator-executor-production | OperatorLifecycleExecutor; RPC only; lifecycle-only ADMISSION_SERVICE binding |
| Admission and authority | limitmark-admission-service-staging; separate ProductionAdmissionAuthority class namespace/object staging-public-inquiries-v1 **requires reviewed staging implementation** | limitmark-admission-service-production; AUTHORITY object production-public-inquiries-v1; epoch phase5c-i1-epoch-1 | Admission owns AUTHORITY; named AuthorityLifecycleOnly and AuthorityLifecycleReadOnly entrypoints; no public lifecycle fetch |
| Request-write R2 credential | Staging-only bucket object write | Production-only bucket object write | No result write, Worker management, token management or Global API Key |
| Result-read R2 credential | Staging-only result object read | Production-only result object read | No request write, result write or provider configuration |
| Offline keypair | Staging-only Ed25519 private/public | Production-only Ed25519 private/public | Private key remains offline signer; public key/fingerprint pinned to environment |
| Cron | Mailbox and observer, proposed one-minute schedules | Mailbox and observer, proposed one-minute schedules | Enable only after binding/IAM/route inspection; timing LIVE NOT VERIFIED |
| Mailbox bindings | REQUEST_BUCKET, RESULT_BUCKET, DISPATCH_GUARD, LIFECYCLE_EXECUTOR, LIFECYCLE_READER | Same names, Production targets | No direct AUTHORITY/ADMISSION_SERVICE |
| Observer bindings | REQUEST_BUCKET, RESULT_BUCKET, LIFECYCLE_READER | Same names, Production targets | No executor, guard, authority or admission mutation entrypoint |
| Executor/admission bindings | Executor ADMISSION_SERVICE -> AuthorityLifecycleOnly; admission AUTHORITY | Same, Production targets | Read-only binding points only to AuthorityLifecycleReadOnly |
| I2 public resources | None inherited from Production; separate synthetic qualification fixtures only if reviewed | limitmark-public-ingress-production, limitmark-admin-gateway-production, P, Q, B-public, B-admin, Access, admission-rpc.limitmark.com | No public route while private lifecycle qualifies; Q has no Production capability |

Do not silently reuse a Production DO namespace/object, R2 bucket, credential, signing key, Worker or service binding in staging. A common Cloudflare account is possible only after account-local binding and IAM proof; separate accounts are optional. Never infer cross-account service binding support.

## Credential custody and capability matrix

The provider administrator creates, scopes, rotates and revokes R2 credentials; the offline key custodian creates, rotates and revokes signing keys under a reviewed trust-key change; Vercel P administrators create/revoke bypass values. Record fingerprint, owner, recipient, environment, issuance/revocation and independent approval in a protected inventory, never secret values in this runbook, repo, terminal history, logs or screenshots. Each row is environment-specific unless marked Production-only. Production/staging values must differ. A revoked R2 credential does **not** cancel an already stored request or dispatched call.

| Principal | Receives / creates | Must never receive |
| --- | --- | --- |
| OFFLINE SIGNER | Creates one environment-specific Ed25519 private key and sealed signed command; supplies only public key/fingerprint and sealed artifact to local operator | R2 credentials, provider administration, Worker deployment keys, other environment key |
| LOCAL OPERATOR | Sealed artifact, pinned manifest, per-environment request-bucket write credential and separate result-bucket read credential, local digest journal; provider administrator issues/rotates/revokes credentials | Signing private key, result write, Worker/DO management, API-token creation, Global API Key, credentials for other environment |
| MAILBOX | Public operator key, REQUEST_BUCKET read and RESULT_BUCKET write bindings, own DISPATCH_GUARD, LIFECYCLE_EXECUTOR, LIFECYCLE_READER; deployment administrator owns binding/rotation | Private signing key, API keys, direct AUTHORITY/ADMISSION_SERVICE, DB, public ingress/bypass secrets |
| OBSERVER | REQUEST_BUCKET read, RESULT_BUCKET write, LIFECYCLE_READER only; deployment administrator controls bindings | Executor, guard, authority, mutation admission, private signing key, R2 client credentials |
| EXECUTOR | Public operator key, one ADMISSION_SERVICE service binding to AuthorityLifecycleOnly; deployment administrator manages version and key | Authority namespace, guard, buckets, signing private key, admission RPC MAC, bypass secrets |
| ADMISSION | AUTHORITY binding, operator public key, Production I2 ADMISSION_CURRENT_RPC_KEY and optional previous key, OIDC metadata; key custodian and deployment administrator rotate release keys | Offline private key, R2 credentials, bypass credentials, Vercel-side ADMISSION_RELEASE_RPC_KEY, DB |
| AUTHORITY | Durable sentinel, releases, nonces/permits, atomic receipts, installed operator public key through admission runtime; provider administrator controls namespace/recovery | Offline private key, R2/client credentials, bypass keys; no independent reset authority |
| PUBLIC SIGNER | Production-only INGRESS_SIGNING_PRIVATE_KEY, INGRESS_IDENTITY_HMAC_KEY, PUBLIC_ORIGIN_SECRET (offline generated), B-public (Vercel generated); rotate with bounded key rollout/revocation | AUTHORITY, admission keys, B-admin, DB, offline lifecycle private key |
| ADMIN GATEWAY | Production-only B-admin (Vercel generated), Access team domain/AUD and exact allowed email; Access admin rotates application policy | B-public, signer keys, AUTHORITY, admission keys, DB, offline lifecycle private key |
| VERCEL P | Production PUBLIC_ORIGIN_SECRET, ADMISSION_RELEASE_RPC_KEY, INGRESS_REQUEST_BINDING_KEY, SUBMISSION_TOKEN_SECRET, public ingress keys; later TURNSTILE_SECRET_KEY/DATABASE_URL/RESEND_API_KEY only by separate launch authorization. Platform may inject B-public; B-admin may be observed on authorized request. P admin rotates/revokes values with corresponding Worker/release rollout | Signer private/HMAC key, authority binding, admission current/previous keys, offline key; B-admin must not be deliberately configured as ordinary app env |
| VERCEL Q | Separate project identity, no Production secrets/bindings; Q admin owns Preview configuration | All Production keys, both bypass values, origin bearer, admission/DB/notification/Turnstile credentials, authority and offline key |

deployment/secret-matrix.json is the exact I2 runtime allowed/forbidden name contract. In particular B-public and B-admin differ and are independently revocable; ADMISSION_RELEASE_RPC_KEY and ADMISSION_CURRENT_RPC_KEY have distinct named placements per that contract. Do not flatten the I3B bucket credentials into Worker secrets: they are S3 object credentials scoped in provider IAM, and their **effective** policies require live negative tests. Ordinary request-bucket scope may allow unwanted keys, deletion or excess storage within that bucket. Fixed-key bounded Worker reads prevent target selection, not storage abuse. No Global API Key. Token policy, revocation latency and provider administration separation are **LIVE NOT VERIFIED**.

## Local command and file contracts

All commands run from the repository root with its pinned package dependencies. These examples are syntax, **not instructions to execute provider calls during this documentation task**. The raw templates fail rendered preflight by design. Rendered config files must reside directly under deployment/ and contain no secret values. The Production executor preflight is:

    npm run authority:executor:preflight -- --config deployment/<reviewed-executor>.jsonc

The three private lifecycle preflights each require a mode before --config:

    npm run authority:lifecycle:preflight -- mailbox --config deployment/<reviewed-mailbox>.jsonc
    npm run authority:lifecycle:preflight -- observer --config deployment/<reviewed-observer>.jsonc
    npm run authority:lifecycle:preflight -- transport --config deployment/<reviewed-transport>.json

The fixed CLI manifest path is deployment/lifecycle-transport.production.json, which does not exist in the repository and must be rendered from deployment/lifecycle-transport.production.template.json, kept outside version control and validated with the transport preflight. Its exact version-1 fields are environment, accountId, requestBucket, resultBucket, authorityId, policyEpoch and operatorPublicKey. The CLI accepts no target override. Each protected R2 credential file is strict UTF-8 JSON without BOM, at most 1,024 bytes, containing **exactly** nonempty string members accessKeyId and secretAccessKey. Request-write and result-read files are separate; never put values in argv, the manifest, rendered Worker vars, repository or logs.

The preparation scripts have no quiet flag of their own. Run them through the package script with npm's --silent option so stdout contains only the one sealed JSON line; capture stdout in an operator-only local variable, require exactly one valid JSON line and a successful exit, then write that line with an explicit UTF-8 encoder **without BOM** to a protected file. Do not use ordinary shell redirection of noisy npm output or a shell-default text encoding. Keep AUTHORITY_OPERATOR_PRIVATE_KEY only in the offline signer process environment, never argv or provider configuration. The Production initialization arguments are:

    npm run --silent authority:init:prepare -- --environment production --authority-id production-public-inquiries-v1 --policy-epoch phase5c-i1-epoch-1 --release-id <reviewed-release> --release-key-id <reviewed-key-id> --confirm-production-authority-initialization

The Production rotation arguments are:

    npm run --silent authority:rotate:prepare -- --environment production --authority-id production-public-inquiries-v1 --policy-epoch phase5c-i1-epoch-1 --current-release-id <current> --next-release-id <next> --next-release-key-id <next-key> --activates-at-ms <reviewed-ms> --previous-retires-at-ms <reviewed-ms> --confirm-production-authority-rotation

The signer must verify the saved file is at most 4,096 bytes, BOM-free UTF-8 and one sealed JSON object before handoff. No command may be submitted after its five-minute freshness window; expiration calls for reconciliation of any prior attempt, never a blind new signature.

For PowerShell, use an operator-owned protected path outside the repository and an explicit encoder, for example after setting the reviewed argument array and signer-only environment variable:

    $sealedLines = @(npm.cmd run --silent authority:init:prepare -- @prepareArgs)
    if ($LASTEXITCODE -ne 0 -or $sealedLines.Count -ne 1) { throw 'preparation failed or produced noisy output' }
    $sealedPath = '<protected absolute path outside repository>'
    [System.IO.File]::WriteAllText($sealedPath, $sealedLines[0], [System.Text.UTF8Encoding]::new($false))

The same capture pattern applies to authority:rotate:prepare. Do not print $sealedLines, and immediately use the bounded --inspect parser on the saved file. This example does not create or install any key.

## Lifecycle operator procedure (after all prerequisite gates)

The only reviewed intended chain is offline sign -> sealed artifact -> pinned local CLI -> private R2 request mailbox -> scheduled mailbox -> durable guard -> executor -> lifecycle-only admission -> authority atomic receipt. The observer separately reads through AuthorityLifecycleReadOnly and writes a bounded reconciliation result. The raw templates are not commands to deploy. Production CLI uses the exact pinned deployment/lifecycle-transport.production.json rendered copy after preflight; no target URL, Worker name, method or account override.

1. Offline signer verifies exact operation, environment, authority, epoch, release/key IDs, freshness and explicit Production intent. Use the quiet preparation and explicit UTF-8-without-BOM file procedure above. Never put the private key or sealed JSON in CLI arguments/logs. For Production, both preparation and submission require their respective explicit Production confirmation flags. Staging must use a separately reviewed staging toolchain, not these Production-pinned scripts.
2. Local operator parses bounded sealed-file metadata with npm run authority:init:submit -- --command <protected-file> --confirm-production --inspect (or npm run authority:rotate:submit with the same flags). This read-only inspector prints INSPECTED, operation, environment, authorityId and policyEpoch. It **does not load the manifest or verify signature authenticity**. Preserve the sealed file. The non-inspect submission path subsequently loads the pinned manifest and verifies the signature before its PUT. The CLI reports canonical command digest and raw-file SHA-256 after its one PUT attempt; record both in a protected journal even if transport is ambiguous. Canonical identity is the digest of the command, not the R2 object key or raw-file hash.
3. Once, use npm run authority:init:submit -- --command <protected-file> --request-credentials <protected-credential-file> --confirm-production (or authority:rotate:submit). It sends one bounded S3 PUT to initialize.json or rotate-release.json and returns UNCONFIRMED even on an accepted upload; acceptance is storage evidence, never commit evidence. Do not retry the PUT, re-sign, make a replacement command, overwrite a pending slot, use a second operator, or start another lifecycle action while outcome is uncertain.
4. npm run authority:result -- --kind lifecycle --digest <64-hex-digest> --result-credentials <protected-result-credential-file> is a **read-only R2 GET**. It validates the bounded result and displays either SUCCESS/ALREADY_APPLIED with digest and receipt, or UNCONFIRMED with a reduced observation. A missing, malformed or mismatched result is UNCONFIRMED. Only a positive exact receipt may justify settlement. No force, rearm or reset exists.
5. After exact positive receipt, npm run authority:settle -- --digest <digest> --request-credentials <protected-credential-file> --confirm-production is a **control/lifecycle mutation**: it PUTs settle.json and may release the guard supervisor latch. Read the settlement result with npm run authority:result -- --kind settlement --digest <digest> --nonce <returned-nonce> --result-credentials <protected-result-credential-file>. Require confirmed SETTLED after exact positive receipt **before another supervised lifecycle command**. A REQUESTED control PUT alone is insufficient. Settlement never deletes/reopens a consumed claim and is not lifecycle commit proof.

No wrangler tail, remote dev, temporary HTTP endpoint, public bearer route, manual DO SQL, dashboard mutation or direct RPC improvisation is a submission mechanism. A failed provider call after a durable guard claim can leave the claim consumed **before any executor call**. Actual non-execution in a fault test does not grant the real operator a retry or a rollback certificate.

## Authoritative reconciliation and suspension

On upload timeout, ambiguous HTTP status, schedule delay, lost result, executor/admission exception, revoked reader token or any UNCONFIRMED status, preserve the sealed file, reported canonical digest/raw hash if available, CLI output and local timestamps. Freeze further lifecycle commands for that environment. Do not re-sign, create a replacement command, submit again, clear the slot, rearm the guard or infer rollback from a negative transport observation. If the reader/reconciliation path is unavailable, preserve available evidence, stop new uploads, suspend mutation processing where safely possible and retain read-only observation where possible; **do not delay containment indefinitely** waiting for reconciliation.

npm run authority:reconcile -- --digest <digest> --request-credentials <protected-credential-file> --confirm-production is a **provider mutation**: it PUTs reconcile.json with a new nonce. The authority operation triggered later through the observer is read-only. The CLI prints REQUESTED or UNCONFIRMED with operation, digest and nonce; it does not return authority state. Preserve that output. Then use the **read-only R2 GET** npm run authority:result -- --kind reconciliation --digest <digest> --nonce <returned-nonce> --result-credentials <protected-result-credential-file>. Preserve its printed JSON and timestamps. Do not confuse REQUESTED with an observed receipt.

The result verifier internally enforces an at-most-8,192-byte strict UTF-8/JSON schema, result version, Production environment/authority/epoch, requested digest/nonce, bounded observation freshness, and receipt shape including key fingerprint, sequence and release fields. For a positive reconciliation it prints SUCCESS, digest and the full validated receipt object. For a negative reconciliation it prints UNCONFIRMED, digest and observation: NOT_FOUND, HISTORY_INCOMPLETE or UNAVAILABLE. It **does not print** the negative observation's initialized flag, coverage or release array; it also does not expose raw R2 bytes, result schema/version, observation timestamp, account ID, or the nonce after verification. The operator can compare the positive receipt's displayed digest, environment, authorityId, policyEpoch, keyFingerprint, release IDs and sequence to the sealed command and separately reviewed manifest/key inventory; the CLI has already validated the requested nonce internally. The displayed receipt is transport data validated by the CLI, not an independently signed attestation. The operator must independently retain deployed reader-binding/version evidence from the pre-initialization gate before trusting it.

**CLOSED / TOOLING REQUIRED:** any later acceptance gate demanding direct operator inspection of negative coverage, uninitialized or current release arrays, raw result bytes, observation time, or guard-claim contents cannot pass with the current CLI output alone. Use only a separately reviewed evidence acquisition tool and re-review that gate; do not use manual SQL, dashboard authority internals, temporary HTTP, direct RPC or mutation. The operator may preserve command artifact, CLI stdout/stderr, digest/raw hash after submission, nonce from the reconcile request, displayed positive receipt and settlement output directly. If the digest was not reported before a failed pre-PUT validation, preserve the artifact and treat digest evidence as unavailable until a reviewed local derivation exists.

An exact positive receipt with matching command digest and target proves that command committed atomically with authority state, even if the current release later changes. Require confirmed positive settlement before another supervised lifecycle command. NOT_FOUND while execution could be in flight remains UNCONFIRMED; negative output does not prove an uninitialized authority or complete receipt coverage to the operator. HISTORY_INCOMPLETE is a recovery gate: stop all lifecycle processing and preserve evidence. UNAVAILABLE remains UNCONFIRMED. Invalid result schema, wrong nonce/digest/environment/authority/epoch or unverified reader binding also remain UNCONFIRMED. Negative evidence never releases the supervisor latch. A token revocation or Cron disable cannot retract stored input. Recovery from suspected PITR/reset or missing history follows PHASE5C_I2_OPERATIONS.md state-loss drain and separate review; it never edits the guard and authority independently.

Safe suspension: preserve available evidence and attempt reconciliation when available; stop new uploads immediately, revoke request-write credentials where warranted, then suspend future schedules/mutation processing where safely possible while retaining read-only observation. Keep durable guard claims and authority receipts intact. Schedule suspension after possible dispatch is availability control, **not rollback**. Never describe deleting a guard claim, restoring the guard alone, clearing an authority receipt, resetting a DO, rearming a command or deleting R2 input as rollback.

## Admission prerequisites before Production initialization

Gate 11A is pre-initialization infrastructure qualification only. Compare the **deployed** admission runtime and secret-name inventory against workers/admission-service/index.ts, deployment/admission-service.template.jsonc, deployment/secret-matrix.json and deployment/secret-policy.ts. The AuthorityLifecycleOnly RPC entrypoint calls validateRuntimeSecrets("admissionService", ...) before reaching AUTHORITY. Thus ADMISSION_CURRENT_RPC_KEY must be installed in admission secret storage even for lifecycle initialization. Verify its presence, format through the reviewed runtime/configuration check, and placement **without displaying its value**. Verify optional ADMISSION_PREVIOUS_RPC_KEY only as part of a complete reviewed previous-release tuple; do not install a partial tuple. Confirm no ingress, bypass, DB, Vercel-side ADMISSION_RELEASE_RPC_KEY or offline private signing key appears in admission.

Verify admission's ADMISSION_CURRENT_RELEASE_ID, ADMISSION_CURRENT_KEY_ID and ADMISSION_CURRENT_ACTIVATED_AT_MS against the exact reviewed release; VERCEL_OIDC_ISSUER, VERCEL_OIDC_AUDIENCE, VERCEL_OIDC_SUBJECT, VERCEL_OWNER_ID and VERCEL_PROJECT_ID against the intended P identity; AUTHORITY_OPERATOR_PUBLIC_KEY and fingerprint against the offline key and executor/mailbox; and AUTHORITY binding against the exact Production namespace/object production-public-inquiries-v1 with policy epoch phase5c-i1-epoch-1. Verify OPERATOR_EXECUTOR_ENVIRONMENT and LIFECYCLE_ENVIRONMENT are production, plus executor ADMISSION_SERVICE -> AuthorityLifecycleOnly and mailbox/observer LIFECYCLE_READER -> AuthorityLifecycleReadOnly. The lifecycle-only method currently checks the secret policy and routes to the fixed authority; it does **not** call configure(), perform Vercel OIDC exchange or verify an admission RPC MAC. The OIDC/MAC values are runtime prerequisites for later public PRE/POST, and ADMISSION_CURRENT_RPC_KEY is already a mandatory lifecycle secret-policy prerequisite. Do not claim OIDC/MAC provider success before its later smoke gate.

Evidence is a redacted provider secret-name list, rendered vars and binding/version inventory, public-key fingerprint comparison, negative placement check and a reviewed non-mutating readiness result where such a path exists. The current Production authority:result CLI does not print the negative inspection's initialized field, so it alone cannot prove the uninitialized state required at Gate 11A. **Gate 11A is CLOSED / TOOLING REQUIRED** if uninitialized state, runtime key presence or entrypoint readiness cannot be positively verified without exposing values or consuming a command. A missing prerequisite discovered only after the guard consumes a digest can leave a consumed claim with no authority receipt. Therefore Gate 12 initialization MUST NOT be submitted on config intent alone.

Intentionally unrecoverable staging faults, especially consumed-before-call, run only in **disposable, independently scoped staging fault fixtures**. Use a separate guard DO namespace/object and, where authority state could be affected, a separate synthetic authority namespace/object, signing key, buckets and pinned bindings from the staging chain needed for later qualification. The fault fixture must use reviewed stage-specific tooling and no Production resource. Preserve redacted artifact/digest, claim/result and dispatch-count evidence. Retire or isolate the whole disposable fixture after review; never delete a consumed claim, reset an authority, or reuse its key/bucket as a shortcut to continue qualification. If independent fixture provisioning or safe evidence collection lacks reviewed tooling, that fault subgate is CLOSED / TOOLING REQUIRED; the main staging environment must not be sacrificed.

## Gate sequence and evidence ledger

For each gate store date, environment, operator/reviewer, exact rendered version and account identity (redacted where needed), actions, PASS artifacts, FAIL reason and suspension/rollback. All provider actions below are future actions; no gate is claimed passed by this document. Gate 0 can be checked locally. Gate 1 is the **first provider action after independent runbook approval**, and is read-only account/plan inventory. No provider mutation before the relevant preceding gate is PASS.

| Gate | Prerequisite; operator action | PASS evidence | FAIL/STOP and suspension |
| --- | --- | --- | --- |
| 0 Repository/checkpoint — LOCAL ONLY | Verify implementation baseline or exact reviewed runbook-only child commit and clean tree; inspect committed I3A/I3B, local secret absence and repository persistence defaults; run npm run validate:worker-config. Run rendered-config preflights only when rendered files later exist, using the exact syntax above | Recorded HEAD/parent, only approved runbook change, no unrelated source change or committed real secret, local persistence defaults closed | Unexpected HEAD/source/secret/default: STOP; no provider action |
| 1 Provider inventory — STRICTLY READ-ONLY | Gate 0; observe Cloudflare/Vercel account identity, plans/entitlements, R2 activation status, quotas/limits, **existing** budgets/alerts, Workers/DO/R2, routes/domains, P/Q, Deployment Protection, DNS/proxy and Access state; record deployed P persistence setting without changing it | Dated complete inventory with evidence for each required field, current resource state and gaps. No mutation occurs. Missing required evidence means NOT VERIFIED, not automatic PASS | Unknown or unavailable evidence: STOP later gates; record missing alerts/budgets for configuration only at a later explicitly authorized mutation gate. Do not activate R2, create anything, configure alerts/budgets, change DNS/Vercel/Access/protection or deploy in Gate 1 |
| 2 Staging manifests/capabilities | Gate 1; separately reviewed staging implementation and schedule-inactive rollout; render staging-only configs/manifest with real target/key fingerprints outside repo; run its reviewed preflights and diff exact binding inventory | Review proves stage-specific admission, authority, CLI, result, executor, reader and guard pins; schedules initially inactive, no placeholders, cross-env reuse, public route or ambient target override | Isolated staging templates/tooling now exist locally (staging mailbox/observer templates ship `crons: []`; Gate 5A preflight requires `requireScheduleState = "STAGING_DEPLOYMENT_INACTIVE"`), so the earlier claim that repository validators/CLI pin only Production and that staging templates carry active Cron no longer reflects committed state; Production's own active-schedule configuration remains separately gated. This local tooling state does not by itself satisfy this gate's own independent-review prerequisite -- this repository cannot independently confirm that review occurred. The operator has since recorded dated live evidence at Gates 4 (2026-09-21) and 5 (2026-09-22) stating staging deployment was performed "using only the reviewed Gate 4B/5A tooling above"; that operator-supplied record is the only basis on which this gate's prerequisite review can be considered satisfied, and it is not independently reproducible from this repository. Discard rendered copies, preserve review artifacts |
| 3 Staging private storage/resources | Gate 2; create private staging request/result R2 buckets, verify public access off, lifecycle 24h/30d and bucket limits; create no schedule yet | Bucket identity/privacy/retention evidence and storage budget | Any public bucket, wrong account or rule: STOP; block upload, preserve objects if ambiguity exists |
| 4 Staging authority/read-only path | Gate 3; deploy stage-specific admission/authority namespace with no public lifecycle route; inspect migrations, fixed object, public fetch closure, AuthorityLifecycleReadOnly read-only behavior and storage durability | Exact namespace/object/version, no reader write, no public entrypoint, restart/read evidence | Binding/schema mismatch or write on read: STOP; suspend admission, preserve state/history |
| 5 Staging executor/mailbox/guard/observer | Gate 4; preflight and deploy executor, mailbox/guard and observer with Cron disabled; inspect every service/R2/DO binding, 404 fetch, routes, workers.dev and preview closure | Versioned binding graph equals inventory; no public endpoints; distinct guard namespace | Extra capability, unresolved binding/version, public URL: STOP; disable schedules/uploads, detach exposure, retain DO state |
| 6 Staging IAM negative tests | Gate 5; administrator issues separate bucket-scoped request-write and result-read credentials; test allowed operation and denied cross-bucket, result write, request write by reader, config/Worker/token management, other environment; test result-read revocation per itemized checks | Provider audit and redacted allow/deny results, no Global API Key; independent policy review | Any overbroad or ineffective revocation: STOP; revoke credential, preserve stored requests |
| 7 Staging initialization/ambiguity | Gate 6 and reviewed stage toolchain; inspect schedule bindings, then enable schedules; sign one synthetic staging initialization; observe PUT acceptance, digest, delay/freshness and exact receipt via read-only reconciliation | One dispatch maximum and exact stage receipt; no cross-env acceptance; measured schedule behavior | Missing receipt/timeout/late action: UNCONFIRMED; suspend new uploads/schedules after reconciliation; preserve claim |
| 8 Staging faults/reconciliation/settlement | Gate 7; run safe duplicate/concurrency/reconciliation tests in the qualified staging chain, and destructive or intentionally stranded faults only in separately pinned disposable staging guard/authority/key/bucket fixtures. Exercise consumed-before-call, lost ack/result, binding/version failure, revocation, restart/eviction, retention and PITR posture; require positive settlement before another supervised command | Instrumented at-most-one dispatch per digest, atomic receipt/history, no hidden retry/read writes, exact receipt and SETTLED output where applicable, preserved disposable-fixture evidence; no claim reset | Any second dispatch, incomplete history, unavailable required evidence/tooling or unproved durability: STOP; close lifecycle, preserve/retire whole fixture without editing claim/receipt |
| 9 STAGING ROTATION QUALIFICATION — CLOSED | Gate 8 plus separately designed, implemented, reviewed stage rotation contract and full stage rotation tests | Currently impossible: staging rotation is NOT IMPLEMENTED | STOP complete staging lifecycle claim and Production provisioning/cutover that requires it. Safe staging initialization and independent provider smoke from gates 7-8 may still proceed if their other gates pass; do not label staging qualified |
| 10 Independent staging acceptance | Gate 9 PASS; independent reviewer checks all stage evidence, consumed-before-call irrecoverability and no-rearm policy | Signed acceptance covering initialization, rotation, ambiguity, IAM, privacy, recovery | Missing gate or reviewer: STOP; no Production I3 provision |
| 11A Production pre-initialization infrastructure | Gate 10; repeat account review; render/preflight Production configs/manifest and reviewed schedule-inactive rollout; create private buckets and distinct authority/guard resources; deploy admission, executor, mailbox and observer with schedules inactive; inspect privacy, URL/route closure, bindings, versions, authority namespace/object, environment/key agreement, read-only **uninitialized** inspection and admission runtime prerequisites above; only after bucket scope exists issue and negatively test narrow credentials; inspect schedule state | Exact Production identity/capability graph, redacted required admission secret-name/config and negative-placement evidence, uninitialized read-only behavior through reviewed evidence tooling, privacy/IAM and route closure. **No command, consumed claim, applied receipt or initialized release state required** | Mismatch, unavailable uninitialized/readiness evidence, overbroad credential, active schedule or exposure: **CLOSED / TOOLING REQUIRED**; block upload, suspend schedules, retain state. No initialization |
| 12 Production initialization | Gate 11A PASS; enable schedules only after final binding/prerequisite inspection; prepare explicitly confirmed Production initialization and make one PUT | CLI transport digest/hash and exact positive initialization receipt after result read or reconciliation; no prior receipt/claim is a prerequisite | Missing prerequisite, UNCONFIRMED/negative/mismatched result: STOP new uploads/commands; preserve artifact and state |
| 12A Production post-initialization lifecycle evidence | Gate 12 exact positive receipt; inspect applied digest/receipt, guard-claim evidence through reviewed tooling, read-only reconciliation and authority continuity after restart; request positive settlement and require SETTLED before another supervised command | Exact receipt and command digest continuity, initialized authority continuity, confirmed settlement, at-most-one dispatch; **CLOSED / TOOLING REQUIRED** for any guard/coverage/current-release evidence not exposed by current CLI | Missing evidence or uncertain state: STOP; no new command, retain guard/authority. Do not infer success from transport |
| 13A Disposable I2 qualification fixtures | Gate 12A and reviewed fixture plan; create isolated disposable application/Preview-like projects, Worker/DO/admission fixtures on disposable hostnames and namespace, separate from P/Q and live limitmark.com routes/customer data; temporarily attach only fixture routes/TLS and fixture Deployment Protection after proving route disjointness and rollback; keep persistence closed | Recorded fixture resource IDs/namespace, synthetic keys/data, exact temporary route/protection scope, no apex/www/admin live route change or unexpected public reachability; fixture readiness before smoke | Route overlap, unprotected public URL, Production credential/data exposure: STOP; detach fixture routes, revoke fixture capabilities and preserve evidence |
| 13B I2 public/admin provider smoke | Gate 13A PASS; exercise signer, gateway, Access, independent fixture bypass values, real fixture OIDC/JWKS, PRE/POST, Host/SNI/TLS, direct URL behavior and applicable provider smoke checks through disposable fixtures only; read-only inspect real Q isolation | Signed fixture ingress/IP/body/Host/SNI and admission OIDC/MAC/DO evidence; Access + requireAdmin denial; Q inventory and fixture Preview isolation; fixture direct URL denial; synthetic only. Retire fixture routes/credentials/resources after evidence capture and verify no residual reachability | Any bypass/Preview leak/stale OIDC: STOP; retire fixtures, keep live routes detached and persistence closed |
| 14 Historical deployment retirement | Gate 13B; inventory every P deployment and effective Production credentials; strip/retire unsafe builds, select reviewed rollback candidates | Old URLs with/without each bypass class cannot reach live Production resources; list of safe rollback builds | Unknown old credential/access: STOP; revoke affected credentials, keep public cutover closed |
| 15 All Deployments and Cloudflare cutover | Gates 13B-14 and **complete reviewed current-state rollback sheet** below; enable and verify P All Deployments, then staged DNS proxy/route/Access attachment per cutover section | All generated URLs protected; signed public/admin paths work, direct URLs deny, no routing loop; restoration actions and verification ready | Missing rollback entry or bypass/access/origin failure: STOP; use traffic rollback sheet, persistence stays closed |
| 16 Observation | Gate 15; synthetic and limited live observation, privacy and alert review | Public and admin chain stable; ENABLE_PERSISTENT_SUBMISSIONS=false in deployed P; no real submission persistence | Incident: detach routes/restore reviewed traffic path, close intake; do not reset lifecycle |
| 17 Later persistence authorization | Gate 16 plus **separate** Turnstile/PostgreSQL/provider/abuse review and explicit launch authorization | Future authorization and all independent persistence gates | No authorization: STOP; I3 completion never opens persistence |

## Live provider checks: required evidence at the named gates

These checks expand gates 1-16. Each row specifies prerequisite, action, PASS, failure stop and suspension. Gate 1 is observation only; any provider-changing action in this table belongs to its later numbered gate. Record redacted evidence and never paste tokens or raw customer data. Synthetic data only for smoke.

| Check / gate | Prerequisite and operator action | PASS evidence | FAIL stop; suspension |
| --- | --- | --- | --- |
| R2 activation, plan, account limits/budgets/alerts (1,3,11A) | At Gate 1 only **observe** activation status, entitlement, quotas and existing alerts/budgets; record absent items. At Gate 3/11A, after separate mutation authorization, configure any required missing alerts/budgets before meaningful uploads | Dated complete read-only inventory, then separately evidenced later configuration where required | Unknown entitlement or missing required alert/budget: Gate 1 NOT VERIFIED or later STOP; no bucket/upload until later gate resolves it |
| Bucket privacy and lifecycle (3,11A) | After each bucket create, inspect public access, custom domains, bindings and 24h input/30d result lifecycle; test actual expiry/consistency with synthetic objects without using expiry as freshness control | No public URL, intended rules and observed read-after-write/retention behavior | Public exposure/rule or consistency anomaly: STOP; block upload, restrict access and investigate objects |
| Request credential scope (6,11A) | Only after request bucket exists, issue single-bucket object credential; test request PUT and denied result write, other bucket, Worker deploy/config/token management | Redacted allowed/denied audit and policy identifier | Any excess right: revoke, STOP |
| Result reader scope and revocation (6,11A) | Only after result bucket exists, issue different single-result-bucket read credential; test result GET and denied request/result writes/config changes; revoke test token and re-test | Denials plus observed revocation timing, independent of request credential | Excess right or ineffective revocation: revoke/block new commands, STOP |
| Rendered bindings and route closure (5,11A) | Inspect deployed Worker versions, named entrypoints, DO/R2/service targets; probe fetch, workers.dev, preview and all route/custom-domain inventories | Exact graph; HTTP lifecycle inaccessible; constant 404 where expected; no unreviewed URL | Any extra route/binding/URL: disable schedule/upload, detach exposure, STOP |
| Environment/key agreement (2,5,6,11A) | Compare manifest, public-key fingerprint, account, bucket, authority, epoch and deployed vars before Production command; negative cross-env tests use staging/disposable fixtures | Exact match and cross-env denial with no Production claim before Gate 12 | Mismatch/reuse: STOP; revoke wrong credential/key placement |
| Cron behavior and delay (7,12,12A) | Enable only after binding inspection; observe real duplicate events, jitter, missed/delayed polls and five-minute freshness with the authorized command or disposable synthetic fixtures | One dispatch/digest; recorded delay distribution; stale command refused without regeneration | Extra dispatch/late accepted command: disable future schedules after preserving evidence, STOP |
| R2 result loss and version failure (8,12A) | Inject absent/corrupt result, unavailable reader, missing binding and wrong Worker version only in disposable synthetic fixture; Production Gate 11A checks compatibility without consuming a command | UNCONFIRMED, no mutation retry; exact receipt recoverable where history exists | False SUCCESS/rollback or re-dispatch: STOP, preserve ledger/receipt |
| Restart, eviction and durability (8,12A) | Use disposable fixtures for fault injection; after Production initialization use non-destructive read-only continuity observation and reviewed tooling, never a reset | Fixture consumed claim/latch/receipt persists; Production exact receipt remains observable; recovery drill never opens empty history | Lost/incomplete history or unavailable evidence: STOP lifecycle, invoke I2 state-loss drain/review |
| Logging and privacy (5-8,11A,13B) | Inspect logs, traces, audit and artifact storage settings using synthetic markers; verify bounded output and retention/access | No signing key, R2 secret, token, full artifact, raw IP, body or customer data in operator/provider logs | Exposure: STOP; revoke affected credential and restrict logs |
| I2 OIDC/JWKS/Access (13B) | After Gate 13A fixture creation, test actual custom-audience Vercel OIDC/JWKS rotation, Access JWT/AUD/identity, gateway denial, PRE/POST MAC | Exact issuer/audience/subject/project/environment, bounded JWKS behavior, Access allow/deny evidence | Any accepted wrong identity or bare bypass: detach fixture routes, revoke fixture credential, STOP |
| Public edge/URL behavior (13B,15-16) | Use disposable fixture hostnames for Gate 13B Host/SNI, IP provenance, body-byte continuity, direct fixture Vercel URL and route checks; use live apex/www only at Gate 15 after rollback sheet | Signed fixture path works and direct fixture URL denies; later live cutover has no recursion/overlap | Origin bypass, invalid Host/SNI or unsafe old URL: retire fixture or restore live traffic per rollback sheet, STOP |

## I2 provider integration and future traffic cutover

Historical inspection only, **reverify before any mutation**: Cloudflare limitmark.com and www.limitmark.com were DNS-only to Vercel; admin.limitmark.com was proxied; Worker Routes was empty; Zero Trust account existed. Vercel showed one project limitmark as P, no Q, Standard Protection, no Automation Bypass secret, valid apex/www/limitmark.vercel.app, and admin.limitmark.com Invalid Configuration. These are not current-state assertions. Account IDs, project IDs, Access application, exact stable P upstream and provider capabilities are OPERATOR INPUT REQUIRED. Leave mail DNS/TXT/MX untouched.

**OBSERVED 2026-09-20 — REVERIFY BEFORE MUTATION.** A later strictly read-only Gate 1 pass recorded the following. This is a dated snapshot, not a timeless assumption; reverify every field immediately before any provider mutation gate. No account ID, payment detail, personal information or secret value is recorded here.

Cloudflare: Workers Free plan active; 0 Workers; 0 Worker Routes; 0 Durable Objects; Durable Objects with SQLite available on the Free plan; observed Free-plan limits were 100,000 Workers requests/day, a maximum of 100 Workers, 5 Cron triggers/account, 100,000 DO requests/day, 13,000 GB-s/day, 5,000,000 SQL rows read/day, 100,000 SQL rows written/day and 5 GB DO SQL storage. R2 is available on this account but **not** subscribed/activated; R2 activation remains a future provider mutation and is explicitly not performed by this observation. Zero Trust Free plan active, with one existing Access application ("Limitmark Admin" on host admin.limitmark.com, policy "Limitmark Admin Only") already present before this Gate 2 documentation task.

Vercel: exactly one project, "limitmark" (P); no Q project exists. The current Production deployment was commit e19c107 and reported Ready. limitmark.com and www.limitmark.com were valid, with www redirecting to the apex; limitmark.vercel.app was valid. admin.limitmark.com was reported Invalid Configuration. Standard Deployment Protection (Vercel Authentication) was enabled; 0 Automation Bypass secrets existed. Historical Production deployments remained retained. Many existing environment variables carried both Production and Preview scope. Public persistence remained closed (ENABLE_PERSISTENT_SUBMISSIONS=false).

None of the above authorizes R2 activation, Worker/DO/bucket creation, DNS/route/Access/Vercel changes, or any other provider mutation; it is Gate 1 evidence only, superseded by the next actual reverification immediately before the gate that consumes it.

P remains one shared Production Vercel application for public and admin; Q must be a separate Preview project with no Production secrets or resources. Verify Q identity and zero Production variables before connecting branch/preview builds. P needs system variables for deployment identity. At its later authorized gate create two independent P Automation Bypass credentials: B-public only to public signer (Vercel may expose selected B-public in P runtime), B-admin only to admin gateway. Neither grants app authorization. Protect **All Deployments**, including historical/generated URLs, before public route cutover. Qualify direct P URL without bypass, then each bypass with the remaining app gates. Configure Cloudflare Access before admin route; gateway must target one operator-confirmed fixed single-label P .vercel.app origin, not admin.limitmark.com or request-selected Host. The admission-rpc custom domain is a service API for authenticated PRE/POST, never lifecycle submission; attach only in its separately reviewed I2 gate. No provider test may silently open Production persistent submissions.

**Gate 13A disposable I2 fixtures, before provider smoke:** after Gate 12A, review an exact fixture manifest, synthetic initialization command path and resource/route diff. Use a disposable Vercel application project distinct from P and Q for project-wide All Deployments/Automation Bypass tests; a disposable Preview-like project where project separation is being tested; disposable signer and admin gateway Workers; a disposable admission Worker with its own synthetic authority namespace/object, release MAC/OIDC identity and public ingress keys; and a disposable Cloudflare Access application/policy. Pin a separate fixture offline key and initialize that synthetic authority through the reviewed isolated lifecycle procedure; require its exact receipt and confirmed settlement before PRE/POST smoke. If that fixture-specific initialization path is absent, Gate 13A is CLOSED / TOOLING REQUIRED. Keep fixture signing/bypass/origin keys and synthetic inquiry identifiers separate from Production. Fixture DNS names must be explicitly selected and proven disjoint from limitmark.com, www.limitmark.com, admin.limitmark.com and admission-rpc.limitmark.com live routing. No live apex/www/admin Worker route may be attached to satisfy smoke. Check TLS issuance, Host/SNI target and route ownership before temporarily attaching **fixture-only** routes/custom domains; check the disposable Vercel project's Deployment Protection scope before temporarily setting it to All Deployments. Never change P protection as a fixture shortcut. A fixture with any unexpected public reachability, overlap or Production data/credential access is STOP.

Gate 13B may then run synthetic provider smoke through those fixture routes. Record resource identities, deployment versions, fixture protection/Access states, exact temporary routes and TLS status, allow/deny results, no persistence writes, and evidence that live limitmark.com routing remained unchanged. Fixture results establish provider behavior for that isolated configuration; they do not by themselves prove P's actual OIDC claims, All Deployments setting or live apex Host/SNI. Carry those exact-P checks forward to Gate 15 before and during cutover. After evidence capture, remove temporary fixture routes/custom domains, revoke fixture bypass/R2 credentials, retire fixture projects/Workers/Access policy according to the reviewed fixture plan, and verify the fixture URLs no longer reach a protected resource. Preserve redacted evidence and DO receipt history for the required review/retention period; do not erase a guard claim or reset an authority as fixture cleanup. If safe fixture creation, teardown or evidence acquisition lacks a reviewed procedure, Gate 13A/B is CLOSED / TOOLING REQUIRED.

Historical deployments: use P dashboard deployment inventory (or a read-only provider listing) to map deployment IDs/URLs/commits/build dates to pre-I1 and pre-I2 code, including branch/generated aliases and reviewed rollback candidates. Inspect effective Production database, admission, bypass and origin credentials for each old build. Current code cannot enforce gates in an older bundle. Strip effective access by provider-supported protection/expiration, resource-level removal, credential revocation/rotation or reviewed retirement; verify each candidate with no bypass and each credential class separately. Keep only reviewed rollback builds with proven safe access. An obscure origin URL is not a security control.

**Gate 15 rollback sheet is mandatory before any cutover mutation.** From fresh Gate 1 observations updated immediately before Gate 15, record the following for **each** item: current before-state, intended after-state, exact provider-dashboard/API restoration action, verification probe after restoration, owner, and propagation/availability caveat. No blank or assumed entry is acceptable:

| Required item | Restoration and verification to make concrete in the reviewed sheet |
| --- | --- |
| Apex limitmark.com proxy state and DNS target | Restore recorded proxy toggle/target; verify authoritative DNS, TLS and direct site availability after TTL/propagation |
| www.limitmark.com proxy state and DNS target | Restore recorded toggle/target and redirect behavior; verify DNS, TLS, redirect after propagation |
| Public Worker routes | Detach/restore each recorded apex/www route and Worker version; verify no overlap or accidental origin bypass |
| Admin gateway route/configuration | Restore recorded admin route, Worker version and fixed upstream; verify Access-to-gateway-to-P path and denial |
| Cloudflare Access admin state | Restore recorded application/policy/AUD and route association; verify allowed and denied identities, account for policy propagation |
| Vercel P Deployment Protection | Restore recorded scope only after historical-access review; verify generated and direct URLs deny unauthorized traffic, account for rollout delay |
| B-public/B-admin relationship | Restore recorded separate credential identifiers/exposure placement or revoke newly introduced ones; verify both bypass paths and no browser exposure; never record values |
| Relevant domain targets | Restore recorded P fixed .vercel.app target and Cloudflare custom-domain/DNS mapping; verify Host/SNI/TLS after propagation |
| Historical deployment access posture | Restore or retain reviewed retirement/revocation state; verify old URLs cannot reach Production resources even with a project bypass credential |

The sheet must identify exact current values and executable restoration steps from the actual dashboards; this template is not a filled rollback plan. Review and sign it before Gate 15 begins. Mail DNS/TXT/MX records remain untouched. No lifecycle reset, receipt deletion or guard-claim deletion is a traffic rollback.

Future cutover order after Gates 13B-14 and the complete rollback sheet: (1) reverify DNS/P/Q and historical inventory; (2) set and verify P All Deployments and both bypass paths with synthetic probes while public routes remain detached, using the sheet if direct-URL behavior fails; (3) attach/prove Access for admin and resolve its Vercel domain configuration; (4) proxy apex/www with Cloudflare and verify Host/SNI and fallback behavior; (5) attach public signer routes limitmark.com/* and www.limitmark.com/* and admin gateway route admin.limitmark.com/* without overlap; (6) probe public/admin/direct Vercel URLs and observe with persistence closed. Do not attach a route before its upstream and denial path are proven. If traffic fails, execute the reviewed sheet in dependency order and re-test direct URLs and Access. Reverting All Deployments to Standard can reopen historical URLs, so only do so after re-evaluating historical access. DNS propagation is not instantaneous. Traffic restoration does not reverse authority initialization or lifecycle dispatch.

## Cost and retention

Normal lifecycle traffic is small; proposed one-minute mailbox and observer polls are about 43,200 scheduled invocations each, 86,400 combined, per 30-day month per environment before other requests. R2 storage/Class A/Class B operations, Cron/Workers CPU and requests, guard and authority SQLite DO requests/duration/storage, existing Cloudflare Access/Workers and Vercel P/Q build/protection usage all need actual account-plan and quota verification at gate 1. Do not treat published generic pricing as this account's entitlement. Stolen request-bucket credentials can create storage/operation costs even though Workers read only bounded fixed keys; use scoped credentials, provider budgets/alerts, rotation/revocation and lifecycle cleanup. Result retention is 30 days; command/receipt history must not be pruned merely to meet R2 retention or cost targets. PostgreSQL and Turnstile are **not required** for private I3 qualification; public persistence remains closed.

## Sequential operator checklist

- [ ] G0 LOCAL: confirm expected implementation HEAD or exact reviewed runbook-only child, no unrelated source, I3A/I3B review, secret absence and repository persistence defaults. **STOP** on mismatch.
- [ ] G1 READ-ONLY: inventory current provider/account/plan/R2/Worker/DO/routes/P/Q/protection/DNS/Access/deployed persistence and existing budgets/alerts. Record gaps; do not mutate. **STOP** later gates if required evidence is missing.
- [ ] G2: obtain separately reviewed staging implementation and render/preflight staging manifests and binding graph. Production-pinned tooling stays closed for staging use by construction (separate scripts, separate validators, separate confirmation flags); the review prerequisite for staging itself is satisfied only by the operator's own dated record (see the Gate 2 table row above and the Gate 4/5 live evidence entries), which this repository cannot independently reproduce.
- [ ] G3: create/verify private staging buckets and retention, plus later-authorized missing alerts/budgets if required, only after G2 PASS.
- [ ] G4: deploy/verify stage admission, separate authority and read-only path only after G3 PASS.
- [ ] G5: deploy/verify stage lifecycle-only executor, mailbox/guard and observer without schedules/routes only after G4 PASS.
- [ ] G6: issue and negatively test separate stage request-write/result-read credentials only after buckets and G5 PASS.
- [ ] G7: inspect bindings, enable schedules, perform one synthetic stage initialization and reconcile its exact receipt. **STOP** on ambiguity.
- [ ] G8: complete safe stage tests plus destructive consumed-before-call/lost-result faults in independently scoped disposable staging fixtures; preserve evidence and require positive settlement before another command. **STOP** on any second dispatch, unavailable evidence or incomplete history.
- [ ] G9: **STAGING ROTATION QUALIFICATION — CLOSED / NOT IMPLEMENTED. STOP** before claiming full staging acceptance or proceeding to Production.
- [ ] G10: independent staging acceptance only after G9 PASS.
- [ ] G11A PRE-INITIALIZATION: render/preflight and provision Production private resources, negative-test IAM, verify admission secret-name/config and exact bindings, uninitialized read-only path, closure and inactive schedules. **No claim/receipt required. STOP** if prerequisites cannot be positively verified.
- [ ] G12 INITIALIZATION: after G11A PASS, enable schedules, sign and submit one explicitly confirmed Production initialization. **STOP** on UNCONFIRMED; preserve evidence.
- [ ] G12A POST-INITIALIZATION: verify exact receipt/digest, continuity, reviewed guard evidence and read-only reconciliation; require confirmed positive settlement. **STOP / TOOLING REQUIRED** for evidence the CLI does not expose.
- [ ] G13A: establish reviewed disposable I2 project/Worker/Access/TLS/route/initialized-synthetic-authority fixtures, with no live apex/www/admin route change. **STOP** on overlap/reachability.
- [ ] G13B: run synthetic I2 signer, gateway, OIDC/JWKS, bypass and admission smoke through fixtures; preserve evidence and retire fixtures.
- [ ] G14: retire/strip historical deployments and approve safe rollback candidates.
- [ ] G15: complete and review the concrete current-state rollback sheet, then verify P All Deployments and execute Cloudflare DNS/route/Access cutover and direct-URL probes. **STOP** without sheet or on bypass.
- [ ] G16: observe traffic and cost/privacy with public persistence still closed.
- [ ] G17: separately authorize and review Turnstile, PostgreSQL and persistence before any later opening.

## Dependency walk and current status

0 -> 1 (local baseline before observational inventory) -> 2 (reviewed stage design) -> 3 (buckets before scoped credentials) -> 4 (stage authority/read-only) -> 5 (stage private Workers/guard) -> 6 (credentials only after bucket scope exists) -> 7 (first stage command creates its claim/receipt) -> 8 (safe tests plus isolated fault fixtures) -> 9 (separately reviewed staging rotation, currently CLOSED) -> 10 (independent full staging acceptance) -> 11A (Production resources, admission prerequisites, no applied receipt/claim) -> 12 (first Production command and initialization) -> 12A (receipt, claim, reconciliation, settlement and continuity after initialization) -> 13A (disposable I2 routes/TLS/protection/initialized synthetic namespace) -> 13B (fixture smoke) -> 14 (historical retirement) -> 15 (reviewed rollback sheet before live traffic cutover) -> 16 (closed-persistence observation) -> 17 (separate persistence authorization).

Thus no receipt precedes initialization; no claim precedes a command; no scoped credential precedes its bucket; no smoke route precedes fixture creation; no Production lifecycle precedes full staging qualification; no traffic cutover precedes the rollback sheet; and Gate 1 has no provider mutation. Gate 11A cannot require initialized release state. Gates 11A and 12A, plus any fixture evidence beyond current CLI exposure, remain **CLOSED / TOOLING REQUIRED** until reviewed acquisition paths exist. G9 (staging rotation) remains CLOSED: it is NOT IMPLEMENTED by present staging code, a code-level fact this repository can verify directly, independent of any operator record. G2's own review prerequisite is, as of the dated Gate 4/5 live evidence above, satisfied only by the operator's own record, not by anything this repository can independently confirm; see the corrected Gate 2 table row and G2 checklist item above -- the earlier statement that G2 remained closed "by present staging implementation status" is no longer accurate once Gates 4 and 5 record staging deployment as having actually occurred. Every live provider state not covered by a dated evidence entry above remains NOT VERIFIED. Strictly read-only Gate 1 inventory remains safe under the review's stated scope; it cannot open later gates merely by being performed.
